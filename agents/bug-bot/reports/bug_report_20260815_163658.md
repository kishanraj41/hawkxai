# Bug Detection Report

**Scan Date:** 2026-08-15T16:36:58.890799  
**Total Bugs Found:** 156

---

## Summary

⚠️  HIGH: 12 high-severity bug(s) found - should fix soon.

## Severity Breakdown

- 🚨 **Critical:** 0
- ⚠️  **High:** 12
- 💡 **Medium:** 144
- 🔸 **Low:** 0

## Category Breakdown

- **Type-Error:** 72
- **Logic:** 70
- **Null-Check:** 11
- **Memory:** 3

## Files with Most Bugs

- `./research_agent.py`: 65 bug(s)
- `./example_usage.py`: 54 bug(s)
- `./cli.py`: 37 bug(s)

## Detailed Bug List


### ⚠️ HIGH (12)

#### ./research_agent.py:65

**Category:** logic  
**Issue:** Assignment in conditional (should be ==)  
**Confidence:** 80%  
**Fix:** Use == for comparison, not = for assignment  

**Code:**
```python
            results = []
            if response.status_code == 200:
                data = response.json()
                
```

---

#### ./research_agent.py:296

**Category:** logic  
**Issue:** Assignment in conditional (should be ==)  
**Confidence:** 80%  
**Fix:** Use == for comparison, not = for assignment  

**Code:**
```python

if __name__ == "__main__":
    main()

```

---

#### ./research_agent.py:213

**Category:** memory  
**Issue:** File not closed - resource leak  
**Confidence:** 80%  
**Fix:** Use "with open(...) as f:" to ensure file is closed  

**Code:**
```python
        
        with open(filepath, 'w', encoding='utf-8') as f:
            json.dump(research_data, f, indent=2, ensure_ascii=False)
        
```

---

#### ./research_agent.py:218

**Category:** memory  
**Issue:** File not closed - resource leak  
**Confidence:** 80%  
**Fix:** Use "with open(...) as f:" to ensure file is closed  

**Code:**
```python
        summary_file = filepath.replace('.json', '_summary.txt')
        with open(summary_file, 'w', encoding='utf-8') as f:
            f.write(research_data['summary'])
    
```

---

#### ./research_agent.py:233

**Category:** memory  
**Issue:** File not closed - resource leak  
**Confidence:** 80%  
**Fix:** Use "with open(...) as f:" to ensure file is closed  

**Code:**
```python
            filepath = os.path.join(self.output_dir, filename)
            with open(filepath, 'r', encoding='utf-8') as f:
                data = json.load(f)
                history.append({
```

---

#### ./cli.py:90

**Category:** logic  
**Issue:** Assignment in conditional (should be ==)  
**Confidence:** 80%  
**Fix:** Use == for comparison, not = for assignment  

**Code:**
```python
    try:
        if args.command == 'research':
            research = agent.research_topic(args.topic, depth=args.depth)
            print("\n" + "="*60)
```

---

#### ./cli.py:96

**Category:** logic  
**Issue:** Assignment in conditional (should be ==)  
**Confidence:** 80%  
**Fix:** Use == for comparison, not = for assignment  

**Code:**
```python
            
        elif args.command == 'search':
            results = agent.search_web(args.query, num_results=args.results)
            print(f"\n🔍 Search Results for: {args.query}\n")
```

---

#### ./cli.py:105

**Category:** logic  
**Issue:** Assignment in conditional (should be ==)  
**Confidence:** 80%  
**Fix:** Use == for comparison, not = for assignment  

**Code:**
```python
                
        elif args.command == 'ask':
            answer = agent.ask_question(args.question)
            print(f"\n💡 Answer:\n{answer}")
```

---

#### ./cli.py:109

**Category:** logic  
**Issue:** Assignment in conditional (should be ==)  
**Confidence:** 80%  
**Fix:** Use == for comparison, not = for assignment  

**Code:**
```python
            
        elif args.command == 'history':
            history = agent.list_research_history()
            if not history:
```

---

#### ./cli.py:122

**Category:** logic  
**Issue:** Assignment in conditional (should be ==)  
**Confidence:** 80%  
**Fix:** Use == for comparison, not = for assignment  

**Code:**
```python
                    
        elif args.command == 'fetch':
            content = agent.fetch_content(args.url)
            if content:
```

---

#### ./cli.py:141

**Category:** logic  
**Issue:** Assignment in conditional (should be ==)  
**Confidence:** 80%  
**Fix:** Use == for comparison, not = for assignment  

**Code:**
```python

if __name__ == '__main__':
    main()

```

---

#### ./example_usage.py:125

**Category:** logic  
**Issue:** Assignment in conditional (should be ==)  
**Confidence:** 80%  
**Fix:** Use == for comparison, not = for assignment  

**Code:**
```python

if __name__ == "__main__":
    main()

```

---


### 💡 MEDIUM (144)

#### ./research_agent.py:62

**Category:** null-check  
**Issue:** Accessing dict without None check  
**Confidence:** 80%  
**Fix:** Check if key exists or use .get() with default  

**Code:**
```python
            url = f"https://api.duckduckgo.com/?q={quote_plus(query)}&format=json"
            response = requests.get(url, headers=headers, timeout=10)
            
            results = []
```

---

#### ./research_agent.py:69

**Category:** null-check  
**Issue:** Accessing dict without None check  
**Confidence:** 80%  
**Fix:** Check if key exists or use .get() with default  

**Code:**
```python
                # Extract related topics
                if data.get('RelatedTopics'):
                    for topic in data['RelatedTopics'][:num_results]:
                        if isinstance(topic, dict) and 'Text' in topic:
```

---

#### ./research_agent.py:73

**Category:** null-check  
**Issue:** Accessing dict without None check  
**Confidence:** 80%  
**Fix:** Check if key exists or use .get() with default  

**Code:**
```python
                            results.append({
                                'title': topic.get('Text', '')[:100],
                                'url': topic.get('FirstURL', ''),
                                'snippet': topic.get('Text', '')
```

---

#### ./research_agent.py:74

**Category:** null-check  
**Issue:** Accessing dict without None check  
**Confidence:** 80%  
**Fix:** Check if key exists or use .get() with default  

**Code:**
```python
                                'title': topic.get('Text', '')[:100],
                                'url': topic.get('FirstURL', ''),
                                'snippet': topic.get('Text', '')
                            })
```

---

#### ./research_agent.py:75

**Category:** null-check  
**Issue:** Accessing dict without None check  
**Confidence:** 80%  
**Fix:** Check if key exists or use .get() with default  

**Code:**
```python
                                'url': topic.get('FirstURL', ''),
                                'snippet': topic.get('Text', '')
                            })
                
```

---

#### ./research_agent.py:79

**Category:** null-check  
**Issue:** Accessing dict without None check  
**Confidence:** 80%  
**Fix:** Check if key exists or use .get() with default  

**Code:**
```python
                # If we got results, add abstract as first result
                if data.get('Abstract') and data.get('AbstractURL'):
                    results.insert(0, {
                        'title': data.get('Heading', query),
```

---

#### ./research_agent.py:79

**Category:** null-check  
**Issue:** Accessing dict without None check  
**Confidence:** 80%  
**Fix:** Check if key exists or use .get() with default  

**Code:**
```python
                # If we got results, add abstract as first result
                if data.get('Abstract') and data.get('AbstractURL'):
                    results.insert(0, {
                        'title': data.get('Heading', query),
```

---

#### ./research_agent.py:81

**Category:** null-check  
**Issue:** Accessing dict without None check  
**Confidence:** 80%  
**Fix:** Check if key exists or use .get() with default  

**Code:**
```python
                    results.insert(0, {
                        'title': data.get('Heading', query),
                        'url': data.get('AbstractURL', ''),
                        'snippet': data.get('Abstract', '')
```

---

#### ./research_agent.py:82

**Category:** null-check  
**Issue:** Accessing dict without None check  
**Confidence:** 80%  
**Fix:** Check if key exists or use .get() with default  

**Code:**
```python
                        'title': data.get('Heading', query),
                        'url': data.get('AbstractURL', ''),
                        'snippet': data.get('Abstract', '')
                    })
```

---

#### ./research_agent.py:83

**Category:** null-check  
**Issue:** Accessing dict without None check  
**Confidence:** 80%  
**Fix:** Check if key exists or use .get() with default  

**Code:**
```python
                        'url': data.get('AbstractURL', ''),
                        'snippet': data.get('Abstract', '')
                    })
                    
```

---

#### ./research_agent.py:122

**Category:** null-check  
**Issue:** Accessing dict without None check  
**Confidence:** 80%  
**Fix:** Check if key exists or use .get() with default  

**Code:**
```python
            }
            response = requests.get(url, headers=headers, timeout=15)
            response.raise_for_status()
            
```

---

#### ./research_agent.py:33

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        self.output_dir = output_dir
        self.research_history = []
        self._ensure_output_dir()
        
```

---

#### ./research_agent.py:41

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
            
    def search_web(self, query: str, num_results: int = 5) -> List[Dict]:
        """
        Search the web using DuckDuckGo's API.
```

---

#### ./research_agent.py:64

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
            
            results = []
            if response.status_code == 200:
                data = response.json()
```

---

#### ./research_agent.py:70

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
                if data.get('RelatedTopics'):
                    for topic in data['RelatedTopics'][:num_results]:
                        if isinstance(topic, dict) and 'Text' in topic:
                            results.append({
```

---

#### ./research_agent.py:73

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
                            results.append({
                                'title': topic.get('Text', '')[:100],
                                'url': topic.get('FirstURL', ''),
                                'snippet': topic.get('Text', '')
```

---

#### ./research_agent.py:95

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
            logging.info(f"✅ Found {len(results)} results")
            return results[:num_results]
            
        except Exception as e:
```

---

#### ./research_agent.py:106

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
    
    def fetch_content(self, url: str) -> Optional[str]:
        """
        Fetch and extract main content from a URL.
```

---

#### ./research_agent.py:126

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
            # Basic text extraction (in production, use BeautifulSoup or newspaper3k)
            content = response.text[:5000]  # Limit content size
            logging.info(f"✅ Fetched {len(content)} characters")
            return content
```

---

#### ./research_agent.py:151

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
            'timestamp': timestamp,
            'search_results': [],
            'sources': []
        }
```

---

#### ./research_agent.py:152

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
            'search_results': [],
            'sources': []
        }
        
```

---

#### ./research_agent.py:157

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        search_results = self.search_web(topic, num_results=depth)
        research_data['search_results'] = search_results
        
        # Phase 2: Fetch content from top results
```

---

#### ./research_agent.py:160

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        # Phase 2: Fetch content from top results
        for idx, result in enumerate(search_results[:depth], 1):
            logging.info(f"\n📄 Processing source {idx}/{depth}")
            
```

---

#### ./research_agent.py:164

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
            source = {
                'title': result['title'],
                'url': result['url'],
                'snippet': result['snippet'],
```

---

#### ./research_agent.py:165

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
                'title': result['title'],
                'url': result['url'],
                'snippet': result['snippet'],
                'content_preview': None
```

---

#### ./research_agent.py:166

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
                'url': result['url'],
                'snippet': result['snippet'],
                'content_preview': None
            }
```

---

#### ./research_agent.py:171

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
            # Fetch full content
            if result['url']:
                content = self.fetch_content(result['url'])
                if content:
```

---

#### ./research_agent.py:172

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
            if result['url']:
                content = self.fetch_content(result['url'])
                if content:
                    source['content_preview'] = content[:1000]
```

---

#### ./research_agent.py:174

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
                if content:
                    source['content_preview'] = content[:1000]
            
            research_data['sources'].append(source)
```

---

#### ./research_agent.py:176

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
            
            research_data['sources'].append(source)
            time.sleep(1)  # Be respectful to servers
        
```

---

#### ./research_agent.py:180

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        # Phase 3: Generate summary
        research_data['summary'] = self._generate_summary(research_data)
        
        # Save research
```

---

#### ./research_agent.py:191

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        """Generate a summary of research findings."""
        topic = research_data['topic']
        num_sources = len(research_data['sources'])
        
```

---

#### ./research_agent.py:192

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        topic = research_data['topic']
        num_sources = len(research_data['sources'])
        
        summary = f"Research Summary for '{topic}'\n"
```

---

#### ./research_agent.py:197

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        summary += f"Total sources analyzed: {num_sources}\n"
        summary += f"Research conducted: {research_data['timestamp']}\n\n"
        
        summary += "Key Sources:\n"
```

---

#### ./research_agent.py:200

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        summary += "Key Sources:\n"
        for idx, source in enumerate(research_data['sources'], 1):
            summary += f"\n{idx}. {source['title']}\n"
            summary += f"   URL: {source['url']}\n"
```

---

#### ./research_agent.py:201

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        for idx, source in enumerate(research_data['sources'], 1):
            summary += f"\n{idx}. {source['title']}\n"
            summary += f"   URL: {source['url']}\n"
            summary += f"   Summary: {source['snippet'][:200]}...\n"
```

---

#### ./research_agent.py:202

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
            summary += f"\n{idx}. {source['title']}\n"
            summary += f"   URL: {source['url']}\n"
            summary += f"   Summary: {source['snippet'][:200]}...\n"
        
```

---

#### ./research_agent.py:203

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
            summary += f"   URL: {source['url']}\n"
            summary += f"   Summary: {source['snippet'][:200]}...\n"
        
        return summary
```

---

#### ./research_agent.py:209

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        """Save research data to JSON file."""
        timestamp = research_data['timestamp'].replace(':', '-').replace('.', '-')
        filename = f"research_{timestamp}.json"
        filepath = os.path.join(self.output_dir, filename)
```

---

#### ./research_agent.py:219

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        with open(summary_file, 'w', encoding='utf-8') as f:
            f.write(research_data['summary'])
    
    def list_research_history(self) -> List[Dict]:
```

---

#### ./research_agent.py:221

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
    
    def list_research_history(self) -> List[Dict]:
        """
        List all past research sessions.
```

---

#### ./research_agent.py:228

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        """
        research_files = [f for f in os.listdir(self.output_dir) if f.endswith('.json')]
        history = []
        
```

---

#### ./research_agent.py:229

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        research_files = [f for f in os.listdir(self.output_dir) if f.endswith('.json')]
        history = []
        
        for filename in sorted(research_files, reverse=True):
```

---

#### ./research_agent.py:236

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
                history.append({
                    'topic': data['topic'],
                    'timestamp': data['timestamp'],
                    'num_sources': len(data['sources']),
```

---

#### ./research_agent.py:237

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
                    'topic': data['topic'],
                    'timestamp': data['timestamp'],
                    'num_sources': len(data['sources']),
                    'filename': filename
```

---

#### ./research_agent.py:238

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
                    'timestamp': data['timestamp'],
                    'num_sources': len(data['sources']),
                    'filename': filename
                })
```

---

#### ./research_agent.py:260

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        # Formulate answer
        answer = f"Based on research from {len(research['sources'])} sources:\n\n"
        
        for idx, source in enumerate(research['sources'], 1):
```

---

#### ./research_agent.py:262

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        
        for idx, source in enumerate(research['sources'], 1):
            answer += f"{idx}. {source['title']}\n"
            answer += f"   {source['snippet'][:200]}...\n"
```

---

#### ./research_agent.py:263

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        for idx, source in enumerate(research['sources'], 1):
            answer += f"{idx}. {source['title']}\n"
            answer += f"   {source['snippet'][:200]}...\n"
            answer += f"   Source: {source['url']}\n\n"
```

---

#### ./research_agent.py:264

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
            answer += f"{idx}. {source['title']}\n"
            answer += f"   {source['snippet'][:200]}...\n"
            answer += f"   Source: {source['url']}\n\n"
        
```

---

#### ./research_agent.py:265

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
            answer += f"   {source['snippet'][:200]}...\n"
            answer += f"   Source: {source['url']}\n\n"
        
        return answer
```

---

#### ./research_agent.py:281

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
    print("\n" + "="*60)
    print(research['summary'])
    print("="*60)
    
```

---

#### ./research_agent.py:293

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
    for item in history:
        print(f"  - {item['topic']} ({item['timestamp']}) - {item['num_sources']} sources")


```

---

#### ./research_agent.py:272

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    """Example usage of the Research Agent."""
    print("🤖 Research Agent Starting...\n")
    
    agent = ResearchAgent()
```

---

#### ./research_agent.py:280

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    
    print("\n" + "="*60)
    print(research['summary'])
    print("="*60)
```

---

#### ./research_agent.py:281

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    print("\n" + "="*60)
    print(research['summary'])
    print("="*60)
    
```

---

#### ./research_agent.py:282

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    print(research['summary'])
    print("="*60)
    
    # Example 2: Ask a question
```

---

#### ./research_agent.py:287

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    answer = agent.ask_question(question)
    print(f"\n{answer}")
    
    # Example 3: Show research history
```

---

#### ./research_agent.py:290

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    # Example 3: Show research history
    print("\n📚 Research History:")
    history = agent.list_research_history()
    for item in history:
```

---

#### ./research_agent.py:293

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    for item in history:
        print(f"  - {item['topic']} ({item['timestamp']}) - {item['num_sources']} sources")


```

---

#### ./cli.py:93

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
            print("\n" + "="*60)
            print(research['summary'])
            print("="*60)
            
```

---

#### ./cli.py:100

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
            for idx, result in enumerate(results, 1):
                print(f"{idx}. {result['title']}")
                print(f"   URL: {result['url']}")
                print(f"   {result['snippet'][:200]}...")
```

---

#### ./cli.py:101

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
                print(f"{idx}. {result['title']}")
                print(f"   URL: {result['url']}")
                print(f"   {result['snippet'][:200]}...")
                print()
```

---

#### ./cli.py:102

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
                print(f"   URL: {result['url']}")
                print(f"   {result['snippet'][:200]}...")
                print()
                
```

---

#### ./cli.py:116

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
                for idx, item in enumerate(history, 1):
                    print(f"{idx}. {item['topic']}")
                    print(f"   Date: {item['timestamp']}")
                    print(f"   Sources: {item['num_sources']}")
```

---

#### ./cli.py:117

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
                    print(f"{idx}. {item['topic']}")
                    print(f"   Date: {item['timestamp']}")
                    print(f"   Sources: {item['num_sources']}")
                    print(f"   File: {item['filename']}")
```

---

#### ./cli.py:118

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
                    print(f"   Date: {item['timestamp']}")
                    print(f"   Sources: {item['num_sources']}")
                    print(f"   File: {item['filename']}")
                    print()
```

---

#### ./cli.py:119

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
                    print(f"   Sources: {item['num_sources']}")
                    print(f"   File: {item['filename']}")
                    print()
                    
```

---

#### ./cli.py:126

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
                print(f"\n📄 Content from {args.url}:\n")
                print(content[:2000])
                if len(content) > 2000:
                    print("\n... (content truncated)")
```

---

#### ./cli.py:92

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
            research = agent.research_topic(args.topic, depth=args.depth)
            print("\n" + "="*60)
            print(research['summary'])
            print("="*60)
```

---

#### ./cli.py:93

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
            print("\n" + "="*60)
            print(research['summary'])
            print("="*60)
            
```

---

#### ./cli.py:94

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
            print(research['summary'])
            print("="*60)
            
        elif args.command == 'search':
```

---

#### ./cli.py:98

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
            results = agent.search_web(args.query, num_results=args.results)
            print(f"\n🔍 Search Results for: {args.query}\n")
            for idx, result in enumerate(results, 1):
                print(f"{idx}. {result['title']}")
```

---

#### ./cli.py:100

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
            for idx, result in enumerate(results, 1):
                print(f"{idx}. {result['title']}")
                print(f"   URL: {result['url']}")
                print(f"   {result['snippet'][:200]}...")
```

---

#### ./cli.py:101

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
                print(f"{idx}. {result['title']}")
                print(f"   URL: {result['url']}")
                print(f"   {result['snippet'][:200]}...")
                print()
```

---

#### ./cli.py:102

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
                print(f"   URL: {result['url']}")
                print(f"   {result['snippet'][:200]}...")
                print()
                
```

---

#### ./cli.py:103

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
                print(f"   {result['snippet'][:200]}...")
                print()
                
        elif args.command == 'ask':
```

---

#### ./cli.py:107

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
            answer = agent.ask_question(args.question)
            print(f"\n💡 Answer:\n{answer}")
            
        elif args.command == 'history':
```

---

#### ./cli.py:112

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
            if not history:
                print("\n📚 No research history found.")
            else:
                print(f"\n📚 Research History ({len(history)} sessions):\n")
```

---

#### ./cli.py:114

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
            else:
                print(f"\n📚 Research History ({len(history)} sessions):\n")
                for idx, item in enumerate(history, 1):
                    print(f"{idx}. {item['topic']}")
```

---

#### ./cli.py:116

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
                for idx, item in enumerate(history, 1):
                    print(f"{idx}. {item['topic']}")
                    print(f"   Date: {item['timestamp']}")
                    print(f"   Sources: {item['num_sources']}")
```

---

#### ./cli.py:117

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
                    print(f"{idx}. {item['topic']}")
                    print(f"   Date: {item['timestamp']}")
                    print(f"   Sources: {item['num_sources']}")
                    print(f"   File: {item['filename']}")
```

---

#### ./cli.py:118

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
                    print(f"   Date: {item['timestamp']}")
                    print(f"   Sources: {item['num_sources']}")
                    print(f"   File: {item['filename']}")
                    print()
```

---

#### ./cli.py:119

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
                    print(f"   Sources: {item['num_sources']}")
                    print(f"   File: {item['filename']}")
                    print()
                    
```

---

#### ./cli.py:120

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
                    print(f"   File: {item['filename']}")
                    print()
                    
        elif args.command == 'fetch':
```

---

#### ./cli.py:125

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
            if content:
                print(f"\n📄 Content from {args.url}:\n")
                print(content[:2000])
                if len(content) > 2000:
```

---

#### ./cli.py:126

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
                print(f"\n📄 Content from {args.url}:\n")
                print(content[:2000])
                if len(content) > 2000:
                    print("\n... (content truncated)")
```

---

#### ./cli.py:128

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
                if len(content) > 2000:
                    print("\n... (content truncated)")
            else:
                print(f"\n⚠️ Failed to fetch content from {args.url}")
```

---

#### ./cli.py:130

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
            else:
                print(f"\n⚠️ Failed to fetch content from {args.url}")
                sys.exit(1)
                
```

---

#### ./cli.py:134

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    except KeyboardInterrupt:
        print("\n\n⚠️ Interrupted by user")
        sys.exit(130)
    except Exception as e:
```

---

#### ./cli.py:137

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    except Exception as e:
        print(f"\n❌ Error: {str(e)}", file=sys.stderr)
        sys.exit(1)

```

---

#### ./example_usage.py:20

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
    print("\n📊 Research Results:")
    print(f"Topic: {research['topic']}")
    print(f"Sources found: {len(research['sources'])}")
    print(f"\nSummary:\n{research['summary']}")
```

---

#### ./example_usage.py:21

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
    print(f"Topic: {research['topic']}")
    print(f"Sources found: {len(research['sources'])}")
    print(f"\nSummary:\n{research['summary']}")

```

---

#### ./example_usage.py:22

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
    print(f"Sources found: {len(research['sources'])}")
    print(f"\nSummary:\n{research['summary']}")


```

---

#### ./example_usage.py:36

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
    for idx, result in enumerate(results, 1):
        print(f"\n{idx}. {result['title']}")
        print(f"   URL: {result['url']}")
        print(f"   Snippet: {result['snippet'][:150]}...")
```

---

#### ./example_usage.py:37

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        print(f"\n{idx}. {result['title']}")
        print(f"   URL: {result['url']}")
        print(f"   Snippet: {result['snippet'][:150]}...")

```

---

#### ./example_usage.py:38

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        print(f"   URL: {result['url']}")
        print(f"   Snippet: {result['snippet'][:150]}...")


```

---

#### ./example_usage.py:68

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        print(f"Content length: {len(content)} characters")
        print(f"Preview:\n{content[:500]}...")


```

---

#### ./example_usage.py:83

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        for item in history:
            print(f"\n  Topic: {item['topic']}")
            print(f"  Date: {item['timestamp']}")
            print(f"  Sources: {item['num_sources']}")
```

---

#### ./example_usage.py:84

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
            print(f"\n  Topic: {item['topic']}")
            print(f"  Date: {item['timestamp']}")
            print(f"  Sources: {item['num_sources']}")
    else:
```

---

#### ./example_usage.py:85

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
            print(f"  Date: {item['timestamp']}")
            print(f"  Sources: {item['num_sources']}")
    else:
        print("\n📚 No research history found yet.")
```

---

#### ./example_usage.py:12

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    """Example: Basic research on a topic"""
    print("="*60)
    print("Example 1: Basic Research")
    print("="*60)
```

---

#### ./example_usage.py:13

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    print("="*60)
    print("Example 1: Basic Research")
    print("="*60)
    
```

---

#### ./example_usage.py:14

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    print("Example 1: Basic Research")
    print("="*60)
    
    agent = ResearchAgent()
```

---

#### ./example_usage.py:19

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    
    print("\n📊 Research Results:")
    print(f"Topic: {research['topic']}")
    print(f"Sources found: {len(research['sources'])}")
```

---

#### ./example_usage.py:20

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    print("\n📊 Research Results:")
    print(f"Topic: {research['topic']}")
    print(f"Sources found: {len(research['sources'])}")
    print(f"\nSummary:\n{research['summary']}")
```

---

#### ./example_usage.py:21

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    print(f"Topic: {research['topic']}")
    print(f"Sources found: {len(research['sources'])}")
    print(f"\nSummary:\n{research['summary']}")

```

---

#### ./example_usage.py:22

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    print(f"Sources found: {len(research['sources'])}")
    print(f"\nSummary:\n{research['summary']}")


```

---

#### ./example_usage.py:27

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    """Example: Simple web search"""
    print("\n" + "="*60)
    print("Example 2: Web Search")
    print("="*60)
```

---

#### ./example_usage.py:28

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    print("\n" + "="*60)
    print("Example 2: Web Search")
    print("="*60)
    
```

---

#### ./example_usage.py:29

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    print("Example 2: Web Search")
    print("="*60)
    
    agent = ResearchAgent()
```

---

#### ./example_usage.py:34

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    
    print("\n🔍 Search Results:")
    for idx, result in enumerate(results, 1):
        print(f"\n{idx}. {result['title']}")
```

---

#### ./example_usage.py:36

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    for idx, result in enumerate(results, 1):
        print(f"\n{idx}. {result['title']}")
        print(f"   URL: {result['url']}")
        print(f"   Snippet: {result['snippet'][:150]}...")
```

---

#### ./example_usage.py:37

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
        print(f"\n{idx}. {result['title']}")
        print(f"   URL: {result['url']}")
        print(f"   Snippet: {result['snippet'][:150]}...")

```

---

#### ./example_usage.py:38

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
        print(f"   URL: {result['url']}")
        print(f"   Snippet: {result['snippet'][:150]}...")


```

---

#### ./example_usage.py:43

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    """Example: Ask a question"""
    print("\n" + "="*60)
    print("Example 3: Question Answering")
    print("="*60)
```

---

#### ./example_usage.py:44

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    print("\n" + "="*60)
    print("Example 3: Question Answering")
    print("="*60)
    
```

---

#### ./example_usage.py:45

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    print("Example 3: Question Answering")
    print("="*60)
    
    agent = ResearchAgent()
```

---

#### ./example_usage.py:51

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    
    print(f"\n❓ Question: {question}")
    print(f"\n💡 Answer:\n{answer}")

```

---

#### ./example_usage.py:52

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    print(f"\n❓ Question: {question}")
    print(f"\n💡 Answer:\n{answer}")


```

---

#### ./example_usage.py:57

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    """Example: Fetch content from a URL"""
    print("\n" + "="*60)
    print("Example 4: Fetch Content")
    print("="*60)
```

---

#### ./example_usage.py:58

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    print("\n" + "="*60)
    print("Example 4: Fetch Content")
    print("="*60)
    
```

---

#### ./example_usage.py:59

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    print("Example 4: Fetch Content")
    print("="*60)
    
    agent = ResearchAgent()
```

---

#### ./example_usage.py:66

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    if content:
        print(f"\n📄 Fetched content from {url}")
        print(f"Content length: {len(content)} characters")
        print(f"Preview:\n{content[:500]}...")
```

---

#### ./example_usage.py:67

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
        print(f"\n📄 Fetched content from {url}")
        print(f"Content length: {len(content)} characters")
        print(f"Preview:\n{content[:500]}...")

```

---

#### ./example_usage.py:68

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
        print(f"Content length: {len(content)} characters")
        print(f"Preview:\n{content[:500]}...")


```

---

#### ./example_usage.py:73

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    """Example: View research history"""
    print("\n" + "="*60)
    print("Example 5: Research History")
    print("="*60)
```

---

#### ./example_usage.py:74

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    print("\n" + "="*60)
    print("Example 5: Research History")
    print("="*60)
    
```

---

#### ./example_usage.py:75

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    print("Example 5: Research History")
    print("="*60)
    
    agent = ResearchAgent()
```

---

#### ./example_usage.py:81

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    if history:
        print(f"\n📚 Found {len(history)} past research sessions:")
        for item in history:
            print(f"\n  Topic: {item['topic']}")
```

---

#### ./example_usage.py:83

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
        for item in history:
            print(f"\n  Topic: {item['topic']}")
            print(f"  Date: {item['timestamp']}")
            print(f"  Sources: {item['num_sources']}")
```

---

#### ./example_usage.py:84

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
            print(f"\n  Topic: {item['topic']}")
            print(f"  Date: {item['timestamp']}")
            print(f"  Sources: {item['num_sources']}")
    else:
```

---

#### ./example_usage.py:85

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
            print(f"  Date: {item['timestamp']}")
            print(f"  Sources: {item['num_sources']}")
    else:
        print("\n📚 No research history found yet.")
```

---

#### ./example_usage.py:87

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    else:
        print("\n📚 No research history found yet.")


```

---

#### ./example_usage.py:92

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    """Example: Use custom output directory"""
    print("\n" + "="*60)
    print("Example 6: Custom Output Directory")
    print("="*60)
```

---

#### ./example_usage.py:93

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    print("\n" + "="*60)
    print("Example 6: Custom Output Directory")
    print("="*60)
    
```

---

#### ./example_usage.py:94

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    print("Example 6: Custom Output Directory")
    print("="*60)
    
    agent = ResearchAgent(output_dir="my_research")
```

---

#### ./example_usage.py:99

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    
    print(f"\n✅ Research saved to: {agent.output_dir}")


```

---

#### ./example_usage.py:104

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    """Run all examples"""
    print("\n🤖 Research Agent - Example Usage\n")
    
    try:
```

---

#### ./example_usage.py:115

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
        
        print("\n" + "="*60)
        print("✅ All examples completed!")
        print("="*60)
```

---

#### ./example_usage.py:116

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
        print("\n" + "="*60)
        print("✅ All examples completed!")
        print("="*60)
        
```

---

#### ./example_usage.py:117

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
        print("✅ All examples completed!")
        print("="*60)
        
    except KeyboardInterrupt:
```

---

#### ./example_usage.py:120

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    except KeyboardInterrupt:
        print("\n\n⚠️ Examples interrupted by user")
    except Exception as e:
        print(f"\n❌ Error running examples: {str(e)}")
```

---

#### ./example_usage.py:122

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    except Exception as e:
        print(f"\n❌ Error running examples: {str(e)}")


```

---


---

*Generated by Bug Bot - Intelligent Bug Detection Agent*
