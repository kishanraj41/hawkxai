# Bug Detection Report

**Scan Date:** 2026-08-15T16:32:36.145291  
**Total Bugs Found:** 390

---

## Summary

🚨 CRITICAL: 13 critical bug(s) found - immediate action required!

## Severity Breakdown

- 🚨 **Critical:** 13
- ⚠️  **High:** 37
- 💡 **Medium:** 330
- 🔸 **Low:** 10

## Category Breakdown

- **Logic:** 215
- **Type-Error:** 112
- **Null-Check:** 36
- **Memory:** 14
- **Security:** 13

## Files with Most Bugs

- `./agents/pr-review-bot/review_bot.py`: 136 bug(s)
- `./agents/bug-bot/bug_bot.py`: 88 bug(s)
- `./research_agent.py`: 75 bug(s)
- `./example_usage.py`: 54 bug(s)
- `./cli.py`: 37 bug(s)

## Detailed Bug List


### 🚨 CRITICAL (13)

#### ./agents/bug-bot/bug_bot.py:64

**Category:** security  
**Issue:** Code injection vulnerability with eval()  
**Confidence:** 80%  
**Fix:** Avoid eval() - use ast.literal_eval() for safe evaluation  

**Code:**
```python
                    # Security vulnerabilities
                    (r'eval\([^)]*\)', 'Code injection vulnerability with eval()', 
                     'Avoid eval() - use ast.literal_eval() for safe evaluation'),
                    
```

---

#### ./agents/bug-bot/bug_bot.py:65

**Category:** security  
**Issue:** Code injection vulnerability with eval()  
**Confidence:** 80%  
**Fix:** Avoid eval() - use ast.literal_eval() for safe evaluation  

**Code:**
```python
                    (r'eval\([^)]*\)', 'Code injection vulnerability with eval()', 
                     'Avoid eval() - use ast.literal_eval() for safe evaluation'),
                    
                    (r'exec\([^)]*\)', 'Code execution vulnerability with exec()',
```

---

#### ./agents/bug-bot/bug_bot.py:65

**Category:** security  
**Issue:** Code injection vulnerability with eval()  
**Confidence:** 80%  
**Fix:** Avoid eval() - use ast.literal_eval() for safe evaluation  

**Code:**
```python
                    (r'eval\([^)]*\)', 'Code injection vulnerability with eval()', 
                     'Avoid eval() - use ast.literal_eval() for safe evaluation'),
                    
                    (r'exec\([^)]*\)', 'Code execution vulnerability with exec()',
```

---

#### ./agents/bug-bot/bug_bot.py:156

**Category:** security  
**Issue:** Code injection vulnerability with eval()  
**Confidence:** 80%  
**Fix:** Avoid eval() - use ast.literal_eval() for safe evaluation  

**Code:**
```python
                'critical': [
                    (r'eval\(', 'Code injection with eval()',
                     'Avoid eval() - use JSON.parse() or safe alternatives'),
                    
```

---

#### ./agents/bug-bot/bug_bot.py:157

**Category:** security  
**Issue:** Code injection vulnerability with eval()  
**Confidence:** 80%  
**Fix:** Avoid eval() - use ast.literal_eval() for safe evaluation  

**Code:**
```python
                    (r'eval\(', 'Code injection with eval()',
                     'Avoid eval() - use JSON.parse() or safe alternatives'),
                    
                    (r'innerHTML\s*=(?!.*sanitize)', 'XSS vulnerability with innerHTML',
```

---

#### ./agents/bug-bot/bug_bot.py:67

**Category:** security  
**Issue:** Code execution vulnerability with exec()  
**Confidence:** 80%  
**Fix:** Avoid exec() - refactor to use safe alternatives  

**Code:**
```python
                    
                    (r'exec\([^)]*\)', 'Code execution vulnerability with exec()',
                     'Avoid exec() - refactor to use safe alternatives'),
                    
```

---

#### ./agents/bug-bot/bug_bot.py:68

**Category:** security  
**Issue:** Code execution vulnerability with exec()  
**Confidence:** 80%  
**Fix:** Avoid exec() - refactor to use safe alternatives  

**Code:**
```python
                    (r'exec\([^)]*\)', 'Code execution vulnerability with exec()',
                     'Avoid exec() - refactor to use safe alternatives'),
                    
                    (r'pickle\.loads?\([^)]*\)', 'Arbitrary code execution with pickle',
```

---

#### ./agents/bug-bot/bug_bot.py:73

**Category:** security  
**Issue:** Shell injection vulnerability  
**Confidence:** 80%  
**Fix:** Use shell=False and pass command as list  

**Code:**
```python
                    
                    (r'shell=True', 'Shell injection vulnerability',
                     'Use shell=False and pass command as list'),
                    
```

---

#### ./agents/pr-review-bot/review_bot.py:148

**Category:** security  
**Issue:** Code injection vulnerability with eval()  
**Confidence:** 80%  
**Fix:** Avoid eval() - use ast.literal_eval() for safe evaluation  

**Code:**
```python
                'critical': [
                    (r'eval\(', 'Avoid using eval() - security risk'),
                    (r'exec\(', 'Avoid using exec() - security risk'),
                    (r'shell=True', 'Avoid shell=True in subprocess - security risk'),
```

---

#### ./agents/pr-review-bot/review_bot.py:164

**Category:** security  
**Issue:** Code injection vulnerability with eval()  
**Confidence:** 80%  
**Fix:** Avoid eval() - use ast.literal_eval() for safe evaluation  

**Code:**
```python
                'critical': [
                    (r'eval\(', 'Avoid using eval() - security risk'),
                    (r'innerHTML\s*=', 'Potential XSS vulnerability with innerHTML'),
                ],
```

---

#### ./agents/pr-review-bot/review_bot.py:149

**Category:** security  
**Issue:** Code execution vulnerability with exec()  
**Confidence:** 80%  
**Fix:** Avoid exec() - refactor to use safe alternatives  

**Code:**
```python
                    (r'eval\(', 'Avoid using eval() - security risk'),
                    (r'exec\(', 'Avoid using exec() - security risk'),
                    (r'shell=True', 'Avoid shell=True in subprocess - security risk'),
                ],
```

---

#### ./agents/pr-review-bot/review_bot.py:150

**Category:** security  
**Issue:** Shell injection vulnerability  
**Confidence:** 80%  
**Fix:** Use shell=False and pass command as list  

**Code:**
```python
                    (r'exec\(', 'Avoid using exec() - security risk'),
                    (r'shell=True', 'Avoid shell=True in subprocess - security risk'),
                ],
                'major': [
```

---

#### ./agents/pr-review-bot/review_bot.py:150

**Category:** security  
**Issue:** Shell injection vulnerability  
**Confidence:** 80%  
**Fix:** Use shell=False and pass command as list  

**Code:**
```python
                    (r'exec\(', 'Avoid using exec() - security risk'),
                    (r'shell=True', 'Avoid shell=True in subprocess - security risk'),
                ],
                'major': [
```

---


### ⚠️ HIGH (37)

#### ./research_agent.py:58

**Category:** logic  
**Issue:** Assignment in conditional (should be ==)  
**Confidence:** 80%  
**Fix:** Use == for comparison, not = for assignment  

**Code:**
```python
            results = []
            if response.status_code == 200:
                data = response.json()
                
```

---

#### ./research_agent.py:289

**Category:** logic  
**Issue:** Assignment in conditional (should be ==)  
**Confidence:** 80%  
**Fix:** Use == for comparison, not = for assignment  

**Code:**
```python

if __name__ == "__main__":
    main()

```

---

#### ./research_agent.py:206

**Category:** memory  
**Issue:** File not closed - resource leak  
**Confidence:** 80%  
**Fix:** Use "with open(...) as f:" to ensure file is closed  

**Code:**
```python
        
        with open(filepath, 'w', encoding='utf-8') as f:
            json.dump(research_data, f, indent=2, ensure_ascii=False)
        
```

---

#### ./research_agent.py:211

**Category:** memory  
**Issue:** File not closed - resource leak  
**Confidence:** 80%  
**Fix:** Use "with open(...) as f:" to ensure file is closed  

**Code:**
```python
        summary_file = filepath.replace('.json', '_summary.txt')
        with open(summary_file, 'w', encoding='utf-8') as f:
            f.write(research_data['summary'])
    
```

---

#### ./research_agent.py:226

**Category:** memory  
**Issue:** File not closed - resource leak  
**Confidence:** 80%  
**Fix:** Use "with open(...) as f:" to ensure file is closed  

**Code:**
```python
            filepath = os.path.join(self.output_dir, filename)
            with open(filepath, 'r', encoding='utf-8') as f:
                data = json.load(f)
                history.append({
```

---

#### ./cli.py:90

**Category:** logic  
**Issue:** Assignment in conditional (should be ==)  
**Confidence:** 80%  
**Fix:** Use == for comparison, not = for assignment  

**Code:**
```python
    try:
        if args.command == 'research':
            research = agent.research_topic(args.topic, depth=args.depth)
            print("\n" + "="*60)
```

---

#### ./cli.py:96

**Category:** logic  
**Issue:** Assignment in conditional (should be ==)  
**Confidence:** 80%  
**Fix:** Use == for comparison, not = for assignment  

**Code:**
```python
            
        elif args.command == 'search':
            results = agent.search_web(args.query, num_results=args.results)
            print(f"\n🔍 Search Results for: {args.query}\n")
```

---

#### ./cli.py:105

**Category:** logic  
**Issue:** Assignment in conditional (should be ==)  
**Confidence:** 80%  
**Fix:** Use == for comparison, not = for assignment  

**Code:**
```python
                
        elif args.command == 'ask':
            answer = agent.ask_question(args.question)
            print(f"\n💡 Answer:\n{answer}")
```

---

#### ./cli.py:109

**Category:** logic  
**Issue:** Assignment in conditional (should be ==)  
**Confidence:** 80%  
**Fix:** Use == for comparison, not = for assignment  

**Code:**
```python
            
        elif args.command == 'history':
            history = agent.list_research_history()
            if not history:
```

---

#### ./cli.py:122

**Category:** logic  
**Issue:** Assignment in conditional (should be ==)  
**Confidence:** 80%  
**Fix:** Use == for comparison, not = for assignment  

**Code:**
```python
                    
        elif args.command == 'fetch':
            content = agent.fetch_content(args.url)
            if content:
```

---

#### ./cli.py:141

**Category:** logic  
**Issue:** Assignment in conditional (should be ==)  
**Confidence:** 80%  
**Fix:** Use == for comparison, not = for assignment  

**Code:**
```python

if __name__ == '__main__':
    main()

```

---

#### ./example_usage.py:125

**Category:** logic  
**Issue:** Assignment in conditional (should be ==)  
**Confidence:** 80%  
**Fix:** Use == for comparison, not = for assignment  

**Code:**
```python

if __name__ == "__main__":
    main()

```

---

#### ./agents/bug-bot/bug_bot.py:245

**Category:** logic  
**Issue:** Assignment in conditional (should be ==)  
**Confidence:** 80%  
**Fix:** Use == for comparison, not = for assignment  

**Code:**
```python
            for pattern in patterns:
                if len(pattern) == 2:
                    regex, description = pattern
                    suggestion = None
```

---

#### ./agents/bug-bot/bug_bot.py:248

**Category:** logic  
**Issue:** Assignment in conditional (should be ==)  
**Confidence:** 80%  
**Fix:** Use == for comparison, not = for assignment  

**Code:**
```python
                    suggestion = None
                elif len(pattern) == 3:
                    regex, description, suggestion = pattern
                else:
```

---

#### ./agents/bug-bot/bug_bot.py:349

**Category:** logic  
**Issue:** Assignment in conditional (should be ==)  
**Confidence:** 80%  
**Fix:** Use == for comparison, not = for assignment  

**Code:**
```python
        severity_counts = {
            'critical': sum(1 for b in bugs if b.severity == 'critical'),
            'high': sum(1 for b in bugs if b.severity == 'high'),
            'medium': sum(1 for b in bugs if b.severity == 'medium'),
```

---

#### ./agents/bug-bot/bug_bot.py:350

**Category:** logic  
**Issue:** Assignment in conditional (should be ==)  
**Confidence:** 80%  
**Fix:** Use == for comparison, not = for assignment  

**Code:**
```python
            'critical': sum(1 for b in bugs if b.severity == 'critical'),
            'high': sum(1 for b in bugs if b.severity == 'high'),
            'medium': sum(1 for b in bugs if b.severity == 'medium'),
            'low': sum(1 for b in bugs if b.severity == 'low'),
```

---

#### ./agents/bug-bot/bug_bot.py:351

**Category:** logic  
**Issue:** Assignment in conditional (should be ==)  
**Confidence:** 80%  
**Fix:** Use == for comparison, not = for assignment  

**Code:**
```python
            'high': sum(1 for b in bugs if b.severity == 'high'),
            'medium': sum(1 for b in bugs if b.severity == 'medium'),
            'low': sum(1 for b in bugs if b.severity == 'low'),
        }
```

---

#### ./agents/bug-bot/bug_bot.py:352

**Category:** logic  
**Issue:** Assignment in conditional (should be ==)  
**Confidence:** 80%  
**Fix:** Use == for comparison, not = for assignment  

**Code:**
```python
            'medium': sum(1 for b in bugs if b.severity == 'medium'),
            'low': sum(1 for b in bugs if b.severity == 'low'),
        }
        
```

---

#### ./agents/bug-bot/bug_bot.py:458

**Category:** logic  
**Issue:** Assignment in conditional (should be ==)  
**Confidence:** 80%  
**Fix:** Use == for comparison, not = for assignment  

**Code:**
```python
            for severity in ['critical', 'high', 'medium', 'low']:
                bugs_in_severity = [b for b in report.all_bugs if b.severity == severity]
                if not bugs_in_severity:
                    continue
```

---

#### ./agents/bug-bot/bug_bot.py:522

**Category:** logic  
**Issue:** Assignment in conditional (should be ==)  
**Confidence:** 80%  
**Fix:** Use == for comparison, not = for assignment  

**Code:**
```python

if __name__ == '__main__':
    main()

```

---

#### ./agents/bug-bot/bug_bot.py:103

**Category:** memory  
**Issue:** File not closed - resource leak  
**Confidence:** 80%  
**Fix:** Use "with open(...) as f:" to ensure file is closed  

**Code:**
```python
                    (r'open\([^)]+\)(?!.*with)', 'File not closed - resource leak',
                     'Use "with open(...) as f:" to ensure file is closed'),
                    
                    # Infinite loops
```

---

#### ./agents/bug-bot/bug_bot.py:201

**Category:** memory  
**Issue:** File not closed - resource leak  
**Confidence:** 80%  
**Fix:** Use "with open(...) as f:" to ensure file is closed  

**Code:**
```python
        if os.path.exists(history_file):
            with open(history_file, 'r') as f:
                return json.load(f)
        return {
```

---

#### ./agents/bug-bot/bug_bot.py:214

**Category:** memory  
**Issue:** File not closed - resource leak  
**Confidence:** 80%  
**Fix:** Use "with open(...) as f:" to ensure file is closed  

**Code:**
```python
        os.makedirs("agents/bug-bot", exist_ok=True)
        with open("agents/bug-bot/bug_history.json", 'w') as f:
            json.dump(self.bug_history, f, indent=2)
    
```

---

#### ./agents/bug-bot/bug_bot.py:222

**Category:** memory  
**Issue:** File not closed - resource leak  
**Confidence:** 80%  
**Fix:** Use "with open(...) as f:" to ensure file is closed  

**Code:**
```python
        try:
            with open(file_path, 'r', encoding='utf-8') as f:
                content = f.read()
        except Exception as e:
```

---

#### ./agents/bug-bot/bug_bot.py:399

**Category:** memory  
**Issue:** File not closed - resource leak  
**Confidence:** 80%  
**Fix:** Use "with open(...) as f:" to ensure file is closed  

**Code:**
```python
        json_file = os.path.join(output_dir, f"bug_report_{timestamp}.json")
        with open(json_file, 'w') as f:
            report_dict = {
                'timestamp': report.timestamp,
```

---

#### ./agents/bug-bot/bug_bot.py:485

**Category:** memory  
**Issue:** File not closed - resource leak  
**Confidence:** 80%  
**Fix:** Use "with open(...) as f:" to ensure file is closed  

**Code:**
```python
        
        with open(filepath, 'w') as f:
            f.write(md)
        
```

---

#### ./agents/pr-review-bot/review_bot.py:191

**Category:** logic  
**Issue:** Assignment in conditional (should be ==)  
**Confidence:** 80%  
**Fix:** Use == for comparison, not = for assignment  

**Code:**
```python
            )
            if result.returncode == 0:
                return json.loads(result.stdout)
            return None
```

---

#### ./agents/pr-review-bot/review_bot.py:205

**Category:** logic  
**Issue:** Assignment in conditional (should be ==)  
**Confidence:** 80%  
**Fix:** Use == for comparison, not = for assignment  

**Code:**
```python
            )
            if result.returncode == 0:
                return result.stdout
            return ""
```

---

#### ./agents/pr-review-bot/review_bot.py:218

**Category:** logic  
**Issue:** Assignment in conditional (should be ==)  
**Confidence:** 80%  
**Fix:** Use == for comparison, not = for assignment  

**Code:**
```python
        language = None
        if file_ext == '.py':
            language = 'python'
        elif file_ext in ['.js', '.jsx', '.ts', '.tsx']:
```

---

#### ./agents/pr-review-bot/review_bot.py:569

**Category:** logic  
**Issue:** Assignment in conditional (should be ==)  
**Confidence:** 80%  
**Fix:** Use == for comparison, not = for assignment  

**Code:**
```python
    
    if sys.argv[1] == 'all':
        # Get all open PRs
        result = subprocess.run(
```

---

#### ./agents/pr-review-bot/review_bot.py:575

**Category:** logic  
**Issue:** Assignment in conditional (should be ==)  
**Confidence:** 80%  
**Fix:** Use == for comparison, not = for assignment  

**Code:**
```python
        )
        if result.returncode == 0:
            prs = json.loads(result.stdout)
            for pr in prs:
```

---

#### ./agents/pr-review-bot/review_bot.py:592

**Category:** logic  
**Issue:** Assignment in conditional (should be ==)  
**Confidence:** 80%  
**Fix:** Use == for comparison, not = for assignment  

**Code:**
```python

if __name__ == '__main__':
    main()

```

---

#### ./agents/pr-review-bot/review_bot.py:51

**Category:** memory  
**Issue:** File not closed - resource leak  
**Confidence:** 80%  
**Fix:** Use "with open(...) as f:" to ensure file is closed  

**Code:**
```python
        if os.path.exists(self.learning_data_path):
            with open(self.learning_data_path, 'r') as f:
                return json.load(f)
        return {
```

---

#### ./agents/pr-review-bot/review_bot.py:64

**Category:** memory  
**Issue:** File not closed - resource leak  
**Confidence:** 80%  
**Fix:** Use "with open(...) as f:" to ensure file is closed  

**Code:**
```python
        os.makedirs(os.path.dirname(self.learning_data_path), exist_ok=True)
        with open(self.learning_data_path, 'w') as f:
            json.dump(self.feedback_history, f, indent=2)
    
```

---

#### ./agents/pr-review-bot/review_bot.py:429

**Category:** memory  
**Issue:** File not closed - resource leak  
**Confidence:** 80%  
**Fix:** Use "with open(...) as f:" to ensure file is closed  

**Code:**
```python
            try:
                with open(os.path.join(self.repo_path, file_path), 'r') as f:
                    content = f.read()
                    file_comments = self.analyze_code_patterns(content, file_path)
```

---

#### ./agents/pr-review-bot/review_bot.py:480

**Category:** memory  
**Issue:** File not closed - resource leak  
**Confidence:** 80%  
**Fix:** Use "with open(...) as f:" to ensure file is closed  

**Code:**
```python
        
        with open(filepath, 'w') as f:
            json.dump(result_dict, f, indent=2)
        
```

---

#### ./agents/pr-review-bot/review_bot.py:552

**Category:** memory  
**Issue:** File not closed - resource leak  
**Confidence:** 80%  
**Fix:** Use "with open(...) as f:" to ensure file is closed  

**Code:**
```python
        
        with open(filepath, 'w') as f:
            f.write(report)
        
```

---


### 💡 MEDIUM (330)

#### ./research_agent.py:55

**Category:** null-check  
**Issue:** Accessing dict without None check  
**Confidence:** 80%  
**Fix:** Check if key exists or use .get() with default  

**Code:**
```python
            url = f"https://api.duckduckgo.com/?q={quote_plus(query)}&format=json"
            response = requests.get(url, headers=headers, timeout=10)
            
            results = []
```

---

#### ./research_agent.py:62

**Category:** null-check  
**Issue:** Accessing dict without None check  
**Confidence:** 80%  
**Fix:** Check if key exists or use .get() with default  

**Code:**
```python
                # Extract related topics
                if data.get('RelatedTopics'):
                    for topic in data['RelatedTopics'][:num_results]:
                        if isinstance(topic, dict) and 'Text' in topic:
```

---

#### ./research_agent.py:66

**Category:** null-check  
**Issue:** Accessing dict without None check  
**Confidence:** 80%  
**Fix:** Check if key exists or use .get() with default  

**Code:**
```python
                            results.append({
                                'title': topic.get('Text', '')[:100],
                                'url': topic.get('FirstURL', ''),
                                'snippet': topic.get('Text', '')
```

---

#### ./research_agent.py:67

**Category:** null-check  
**Issue:** Accessing dict without None check  
**Confidence:** 80%  
**Fix:** Check if key exists or use .get() with default  

**Code:**
```python
                                'title': topic.get('Text', '')[:100],
                                'url': topic.get('FirstURL', ''),
                                'snippet': topic.get('Text', '')
                            })
```

---

#### ./research_agent.py:68

**Category:** null-check  
**Issue:** Accessing dict without None check  
**Confidence:** 80%  
**Fix:** Check if key exists or use .get() with default  

**Code:**
```python
                                'url': topic.get('FirstURL', ''),
                                'snippet': topic.get('Text', '')
                            })
                
```

---

#### ./research_agent.py:72

**Category:** null-check  
**Issue:** Accessing dict without None check  
**Confidence:** 80%  
**Fix:** Check if key exists or use .get() with default  

**Code:**
```python
                # If we got results, add abstract as first result
                if data.get('Abstract') and data.get('AbstractURL'):
                    results.insert(0, {
                        'title': data.get('Heading', query),
```

---

#### ./research_agent.py:72

**Category:** null-check  
**Issue:** Accessing dict without None check  
**Confidence:** 80%  
**Fix:** Check if key exists or use .get() with default  

**Code:**
```python
                # If we got results, add abstract as first result
                if data.get('Abstract') and data.get('AbstractURL'):
                    results.insert(0, {
                        'title': data.get('Heading', query),
```

---

#### ./research_agent.py:74

**Category:** null-check  
**Issue:** Accessing dict without None check  
**Confidence:** 80%  
**Fix:** Check if key exists or use .get() with default  

**Code:**
```python
                    results.insert(0, {
                        'title': data.get('Heading', query),
                        'url': data.get('AbstractURL', ''),
                        'snippet': data.get('Abstract', '')
```

---

#### ./research_agent.py:75

**Category:** null-check  
**Issue:** Accessing dict without None check  
**Confidence:** 80%  
**Fix:** Check if key exists or use .get() with default  

**Code:**
```python
                        'title': data.get('Heading', query),
                        'url': data.get('AbstractURL', ''),
                        'snippet': data.get('Abstract', '')
                    })
```

---

#### ./research_agent.py:76

**Category:** null-check  
**Issue:** Accessing dict without None check  
**Confidence:** 80%  
**Fix:** Check if key exists or use .get() with default  

**Code:**
```python
                        'url': data.get('AbstractURL', ''),
                        'snippet': data.get('Abstract', '')
                    })
                    
```

---

#### ./research_agent.py:115

**Category:** null-check  
**Issue:** Accessing dict without None check  
**Confidence:** 80%  
**Fix:** Check if key exists or use .get() with default  

**Code:**
```python
            }
            response = requests.get(url, headers=headers, timeout=15)
            response.raise_for_status()
            
```

---

#### ./research_agent.py:26

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        self.output_dir = output_dir
        self.research_history = []
        self._ensure_output_dir()
        
```

---

#### ./research_agent.py:34

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
            
    def search_web(self, query: str, num_results: int = 5) -> List[Dict]:
        """
        Search the web using DuckDuckGo's API.
```

---

#### ./research_agent.py:57

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
            
            results = []
            if response.status_code == 200:
                data = response.json()
```

---

#### ./research_agent.py:63

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
                if data.get('RelatedTopics'):
                    for topic in data['RelatedTopics'][:num_results]:
                        if isinstance(topic, dict) and 'Text' in topic:
                            results.append({
```

---

#### ./research_agent.py:66

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
                            results.append({
                                'title': topic.get('Text', '')[:100],
                                'url': topic.get('FirstURL', ''),
                                'snippet': topic.get('Text', '')
```

---

#### ./research_agent.py:88

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
            print(f"✅ Found {len(results)} results")
            return results[:num_results]
            
        except Exception as e:
```

---

#### ./research_agent.py:99

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
    
    def fetch_content(self, url: str) -> Optional[str]:
        """
        Fetch and extract main content from a URL.
```

---

#### ./research_agent.py:119

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
            # Basic text extraction (in production, use BeautifulSoup or newspaper3k)
            content = response.text[:5000]  # Limit content size
            print(f"✅ Fetched {len(content)} characters")
            return content
```

---

#### ./research_agent.py:144

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
            'timestamp': timestamp,
            'search_results': [],
            'sources': []
        }
```

---

#### ./research_agent.py:145

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
            'search_results': [],
            'sources': []
        }
        
```

---

#### ./research_agent.py:150

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        search_results = self.search_web(topic, num_results=depth)
        research_data['search_results'] = search_results
        
        # Phase 2: Fetch content from top results
```

---

#### ./research_agent.py:153

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        # Phase 2: Fetch content from top results
        for idx, result in enumerate(search_results[:depth], 1):
            print(f"\n📄 Processing source {idx}/{depth}")
            
```

---

#### ./research_agent.py:157

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
            source = {
                'title': result['title'],
                'url': result['url'],
                'snippet': result['snippet'],
```

---

#### ./research_agent.py:158

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
                'title': result['title'],
                'url': result['url'],
                'snippet': result['snippet'],
                'content_preview': None
```

---

#### ./research_agent.py:159

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
                'url': result['url'],
                'snippet': result['snippet'],
                'content_preview': None
            }
```

---

#### ./research_agent.py:164

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
            # Fetch full content
            if result['url']:
                content = self.fetch_content(result['url'])
                if content:
```

---

#### ./research_agent.py:165

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
            if result['url']:
                content = self.fetch_content(result['url'])
                if content:
                    source['content_preview'] = content[:1000]
```

---

#### ./research_agent.py:167

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
                if content:
                    source['content_preview'] = content[:1000]
            
            research_data['sources'].append(source)
```

---

#### ./research_agent.py:169

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
            
            research_data['sources'].append(source)
            time.sleep(1)  # Be respectful to servers
        
```

---

#### ./research_agent.py:173

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        # Phase 3: Generate summary
        research_data['summary'] = self._generate_summary(research_data)
        
        # Save research
```

---

#### ./research_agent.py:184

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        """Generate a summary of research findings."""
        topic = research_data['topic']
        num_sources = len(research_data['sources'])
        
```

---

#### ./research_agent.py:185

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        topic = research_data['topic']
        num_sources = len(research_data['sources'])
        
        summary = f"Research Summary for '{topic}'\n"
```

---

#### ./research_agent.py:190

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        summary += f"Total sources analyzed: {num_sources}\n"
        summary += f"Research conducted: {research_data['timestamp']}\n\n"
        
        summary += "Key Sources:\n"
```

---

#### ./research_agent.py:193

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        summary += "Key Sources:\n"
        for idx, source in enumerate(research_data['sources'], 1):
            summary += f"\n{idx}. {source['title']}\n"
            summary += f"   URL: {source['url']}\n"
```

---

#### ./research_agent.py:194

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        for idx, source in enumerate(research_data['sources'], 1):
            summary += f"\n{idx}. {source['title']}\n"
            summary += f"   URL: {source['url']}\n"
            summary += f"   Summary: {source['snippet'][:200]}...\n"
```

---

#### ./research_agent.py:195

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
            summary += f"\n{idx}. {source['title']}\n"
            summary += f"   URL: {source['url']}\n"
            summary += f"   Summary: {source['snippet'][:200]}...\n"
        
```

---

#### ./research_agent.py:196

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
            summary += f"   URL: {source['url']}\n"
            summary += f"   Summary: {source['snippet'][:200]}...\n"
        
        return summary
```

---

#### ./research_agent.py:202

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        """Save research data to JSON file."""
        timestamp = research_data['timestamp'].replace(':', '-').replace('.', '-')
        filename = f"research_{timestamp}.json"
        filepath = os.path.join(self.output_dir, filename)
```

---

#### ./research_agent.py:212

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        with open(summary_file, 'w', encoding='utf-8') as f:
            f.write(research_data['summary'])
    
    def list_research_history(self) -> List[Dict]:
```

---

#### ./research_agent.py:214

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
    
    def list_research_history(self) -> List[Dict]:
        """
        List all past research sessions.
```

---

#### ./research_agent.py:221

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        """
        research_files = [f for f in os.listdir(self.output_dir) if f.endswith('.json')]
        history = []
        
```

---

#### ./research_agent.py:222

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        research_files = [f for f in os.listdir(self.output_dir) if f.endswith('.json')]
        history = []
        
        for filename in sorted(research_files, reverse=True):
```

---

#### ./research_agent.py:229

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
                history.append({
                    'topic': data['topic'],
                    'timestamp': data['timestamp'],
                    'num_sources': len(data['sources']),
```

---

#### ./research_agent.py:230

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
                    'topic': data['topic'],
                    'timestamp': data['timestamp'],
                    'num_sources': len(data['sources']),
                    'filename': filename
```

---

#### ./research_agent.py:231

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
                    'timestamp': data['timestamp'],
                    'num_sources': len(data['sources']),
                    'filename': filename
                })
```

---

#### ./research_agent.py:253

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        # Formulate answer
        answer = f"Based on research from {len(research['sources'])} sources:\n\n"
        
        for idx, source in enumerate(research['sources'], 1):
```

---

#### ./research_agent.py:255

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        
        for idx, source in enumerate(research['sources'], 1):
            answer += f"{idx}. {source['title']}\n"
            answer += f"   {source['snippet'][:200]}...\n"
```

---

#### ./research_agent.py:256

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        for idx, source in enumerate(research['sources'], 1):
            answer += f"{idx}. {source['title']}\n"
            answer += f"   {source['snippet'][:200]}...\n"
            answer += f"   Source: {source['url']}\n\n"
```

---

#### ./research_agent.py:257

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
            answer += f"{idx}. {source['title']}\n"
            answer += f"   {source['snippet'][:200]}...\n"
            answer += f"   Source: {source['url']}\n\n"
        
```

---

#### ./research_agent.py:258

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
            answer += f"   {source['snippet'][:200]}...\n"
            answer += f"   Source: {source['url']}\n\n"
        
        return answer
```

---

#### ./research_agent.py:274

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
    print("\n" + "="*60)
    print(research['summary'])
    print("="*60)
    
```

---

#### ./research_agent.py:286

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
    for item in history:
        print(f"  - {item['topic']} ({item['timestamp']}) - {item['num_sources']} sources")


```

---

#### ./research_agent.py:45

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
        """
        print(f"🔍 Searching for: {query}")
        
        # Using DuckDuckGo HTML search (no API key needed)
```

---

#### ./research_agent.py:87

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
                
            print(f"✅ Found {len(results)} results")
            return results[:num_results]
            
```

---

#### ./research_agent.py:91

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
        except Exception as e:
            print(f"⚠️ Search error: {str(e)}")
            return [{
                'title': f'Search for: {query}',
```

---

#### ./research_agent.py:109

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
        """
        print(f"📥 Fetching content from: {url}")
        
        try:
```

---

#### ./research_agent.py:120

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
            content = response.text[:5000]  # Limit content size
            print(f"✅ Fetched {len(content)} characters")
            return content
            
```

---

#### ./research_agent.py:124

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
        except Exception as e:
            print(f"⚠️ Fetch error: {str(e)}")
            return None
    
```

---

#### ./research_agent.py:138

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
        """
        print(f"\n🎯 Starting research on: {topic}\n")
        
        timestamp = datetime.now().isoformat()
```

---

#### ./research_agent.py:154

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
        for idx, result in enumerate(search_results[:depth], 1):
            print(f"\n📄 Processing source {idx}/{depth}")
            
            source = {
```

---

#### ./research_agent.py:179

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
        
        print(f"\n✅ Research completed! Results saved to {self.output_dir}")
        return research_data
    
```

---

#### ./research_agent.py:247

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
        """
        print(f"\n❓ Question: {question}\n")
        
        # Research the question
```

---

#### ./research_agent.py:265

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    """Example usage of the Research Agent."""
    print("🤖 Research Agent Starting...\n")
    
    agent = ResearchAgent()
```

---

#### ./research_agent.py:273

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    
    print("\n" + "="*60)
    print(research['summary'])
    print("="*60)
```

---

#### ./research_agent.py:274

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    print("\n" + "="*60)
    print(research['summary'])
    print("="*60)
    
```

---

#### ./research_agent.py:275

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    print(research['summary'])
    print("="*60)
    
    # Example 2: Ask a question
```

---

#### ./research_agent.py:280

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    answer = agent.ask_question(question)
    print(f"\n{answer}")
    
    # Example 3: Show research history
```

---

#### ./research_agent.py:283

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    # Example 3: Show research history
    print("\n📚 Research History:")
    history = agent.list_research_history()
    for item in history:
```

---

#### ./research_agent.py:286

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    for item in history:
        print(f"  - {item['topic']} ({item['timestamp']}) - {item['num_sources']} sources")


```

---

#### ./cli.py:93

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
            print("\n" + "="*60)
            print(research['summary'])
            print("="*60)
            
```

---

#### ./cli.py:100

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
            for idx, result in enumerate(results, 1):
                print(f"{idx}. {result['title']}")
                print(f"   URL: {result['url']}")
                print(f"   {result['snippet'][:200]}...")
```

---

#### ./cli.py:101

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
                print(f"{idx}. {result['title']}")
                print(f"   URL: {result['url']}")
                print(f"   {result['snippet'][:200]}...")
                print()
```

---

#### ./cli.py:102

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
                print(f"   URL: {result['url']}")
                print(f"   {result['snippet'][:200]}...")
                print()
                
```

---

#### ./cli.py:116

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
                for idx, item in enumerate(history, 1):
                    print(f"{idx}. {item['topic']}")
                    print(f"   Date: {item['timestamp']}")
                    print(f"   Sources: {item['num_sources']}")
```

---

#### ./cli.py:117

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
                    print(f"{idx}. {item['topic']}")
                    print(f"   Date: {item['timestamp']}")
                    print(f"   Sources: {item['num_sources']}")
                    print(f"   File: {item['filename']}")
```

---

#### ./cli.py:118

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
                    print(f"   Date: {item['timestamp']}")
                    print(f"   Sources: {item['num_sources']}")
                    print(f"   File: {item['filename']}")
                    print()
```

---

#### ./cli.py:119

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
                    print(f"   Sources: {item['num_sources']}")
                    print(f"   File: {item['filename']}")
                    print()
                    
```

---

#### ./cli.py:126

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
                print(f"\n📄 Content from {args.url}:\n")
                print(content[:2000])
                if len(content) > 2000:
                    print("\n... (content truncated)")
```

---

#### ./cli.py:92

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
            research = agent.research_topic(args.topic, depth=args.depth)
            print("\n" + "="*60)
            print(research['summary'])
            print("="*60)
```

---

#### ./cli.py:93

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
            print("\n" + "="*60)
            print(research['summary'])
            print("="*60)
            
```

---

#### ./cli.py:94

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
            print(research['summary'])
            print("="*60)
            
        elif args.command == 'search':
```

---

#### ./cli.py:98

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
            results = agent.search_web(args.query, num_results=args.results)
            print(f"\n🔍 Search Results for: {args.query}\n")
            for idx, result in enumerate(results, 1):
                print(f"{idx}. {result['title']}")
```

---

#### ./cli.py:100

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
            for idx, result in enumerate(results, 1):
                print(f"{idx}. {result['title']}")
                print(f"   URL: {result['url']}")
                print(f"   {result['snippet'][:200]}...")
```

---

#### ./cli.py:101

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
                print(f"{idx}. {result['title']}")
                print(f"   URL: {result['url']}")
                print(f"   {result['snippet'][:200]}...")
                print()
```

---

#### ./cli.py:102

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
                print(f"   URL: {result['url']}")
                print(f"   {result['snippet'][:200]}...")
                print()
                
```

---

#### ./cli.py:103

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
                print(f"   {result['snippet'][:200]}...")
                print()
                
        elif args.command == 'ask':
```

---

#### ./cli.py:107

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
            answer = agent.ask_question(args.question)
            print(f"\n💡 Answer:\n{answer}")
            
        elif args.command == 'history':
```

---

#### ./cli.py:112

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
            if not history:
                print("\n📚 No research history found.")
            else:
                print(f"\n📚 Research History ({len(history)} sessions):\n")
```

---

#### ./cli.py:114

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
            else:
                print(f"\n📚 Research History ({len(history)} sessions):\n")
                for idx, item in enumerate(history, 1):
                    print(f"{idx}. {item['topic']}")
```

---

#### ./cli.py:116

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
                for idx, item in enumerate(history, 1):
                    print(f"{idx}. {item['topic']}")
                    print(f"   Date: {item['timestamp']}")
                    print(f"   Sources: {item['num_sources']}")
```

---

#### ./cli.py:117

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
                    print(f"{idx}. {item['topic']}")
                    print(f"   Date: {item['timestamp']}")
                    print(f"   Sources: {item['num_sources']}")
                    print(f"   File: {item['filename']}")
```

---

#### ./cli.py:118

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
                    print(f"   Date: {item['timestamp']}")
                    print(f"   Sources: {item['num_sources']}")
                    print(f"   File: {item['filename']}")
                    print()
```

---

#### ./cli.py:119

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
                    print(f"   Sources: {item['num_sources']}")
                    print(f"   File: {item['filename']}")
                    print()
                    
```

---

#### ./cli.py:120

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
                    print(f"   File: {item['filename']}")
                    print()
                    
        elif args.command == 'fetch':
```

---

#### ./cli.py:125

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
            if content:
                print(f"\n📄 Content from {args.url}:\n")
                print(content[:2000])
                if len(content) > 2000:
```

---

#### ./cli.py:126

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
                print(f"\n📄 Content from {args.url}:\n")
                print(content[:2000])
                if len(content) > 2000:
                    print("\n... (content truncated)")
```

---

#### ./cli.py:128

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
                if len(content) > 2000:
                    print("\n... (content truncated)")
            else:
                print(f"\n⚠️ Failed to fetch content from {args.url}")
```

---

#### ./cli.py:130

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
            else:
                print(f"\n⚠️ Failed to fetch content from {args.url}")
                sys.exit(1)
                
```

---

#### ./cli.py:134

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    except KeyboardInterrupt:
        print("\n\n⚠️ Interrupted by user")
        sys.exit(130)
    except Exception as e:
```

---

#### ./cli.py:137

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    except Exception as e:
        print(f"\n❌ Error: {str(e)}", file=sys.stderr)
        sys.exit(1)

```

---

#### ./example_usage.py:20

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
    print("\n📊 Research Results:")
    print(f"Topic: {research['topic']}")
    print(f"Sources found: {len(research['sources'])}")
    print(f"\nSummary:\n{research['summary']}")
```

---

#### ./example_usage.py:21

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
    print(f"Topic: {research['topic']}")
    print(f"Sources found: {len(research['sources'])}")
    print(f"\nSummary:\n{research['summary']}")

```

---

#### ./example_usage.py:22

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
    print(f"Sources found: {len(research['sources'])}")
    print(f"\nSummary:\n{research['summary']}")


```

---

#### ./example_usage.py:36

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
    for idx, result in enumerate(results, 1):
        print(f"\n{idx}. {result['title']}")
        print(f"   URL: {result['url']}")
        print(f"   Snippet: {result['snippet'][:150]}...")
```

---

#### ./example_usage.py:37

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        print(f"\n{idx}. {result['title']}")
        print(f"   URL: {result['url']}")
        print(f"   Snippet: {result['snippet'][:150]}...")

```

---

#### ./example_usage.py:38

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        print(f"   URL: {result['url']}")
        print(f"   Snippet: {result['snippet'][:150]}...")


```

---

#### ./example_usage.py:68

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        print(f"Content length: {len(content)} characters")
        print(f"Preview:\n{content[:500]}...")


```

---

#### ./example_usage.py:83

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        for item in history:
            print(f"\n  Topic: {item['topic']}")
            print(f"  Date: {item['timestamp']}")
            print(f"  Sources: {item['num_sources']}")
```

---

#### ./example_usage.py:84

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
            print(f"\n  Topic: {item['topic']}")
            print(f"  Date: {item['timestamp']}")
            print(f"  Sources: {item['num_sources']}")
    else:
```

---

#### ./example_usage.py:85

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
            print(f"  Date: {item['timestamp']}")
            print(f"  Sources: {item['num_sources']}")
    else:
        print("\n📚 No research history found yet.")
```

---

#### ./example_usage.py:12

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    """Example: Basic research on a topic"""
    print("="*60)
    print("Example 1: Basic Research")
    print("="*60)
```

---

#### ./example_usage.py:13

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    print("="*60)
    print("Example 1: Basic Research")
    print("="*60)
    
```

---

#### ./example_usage.py:14

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    print("Example 1: Basic Research")
    print("="*60)
    
    agent = ResearchAgent()
```

---

#### ./example_usage.py:19

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    
    print("\n📊 Research Results:")
    print(f"Topic: {research['topic']}")
    print(f"Sources found: {len(research['sources'])}")
```

---

#### ./example_usage.py:20

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    print("\n📊 Research Results:")
    print(f"Topic: {research['topic']}")
    print(f"Sources found: {len(research['sources'])}")
    print(f"\nSummary:\n{research['summary']}")
```

---

#### ./example_usage.py:21

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    print(f"Topic: {research['topic']}")
    print(f"Sources found: {len(research['sources'])}")
    print(f"\nSummary:\n{research['summary']}")

```

---

#### ./example_usage.py:22

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    print(f"Sources found: {len(research['sources'])}")
    print(f"\nSummary:\n{research['summary']}")


```

---

#### ./example_usage.py:27

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    """Example: Simple web search"""
    print("\n" + "="*60)
    print("Example 2: Web Search")
    print("="*60)
```

---

#### ./example_usage.py:28

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    print("\n" + "="*60)
    print("Example 2: Web Search")
    print("="*60)
    
```

---

#### ./example_usage.py:29

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    print("Example 2: Web Search")
    print("="*60)
    
    agent = ResearchAgent()
```

---

#### ./example_usage.py:34

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    
    print("\n🔍 Search Results:")
    for idx, result in enumerate(results, 1):
        print(f"\n{idx}. {result['title']}")
```

---

#### ./example_usage.py:36

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    for idx, result in enumerate(results, 1):
        print(f"\n{idx}. {result['title']}")
        print(f"   URL: {result['url']}")
        print(f"   Snippet: {result['snippet'][:150]}...")
```

---

#### ./example_usage.py:37

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
        print(f"\n{idx}. {result['title']}")
        print(f"   URL: {result['url']}")
        print(f"   Snippet: {result['snippet'][:150]}...")

```

---

#### ./example_usage.py:38

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
        print(f"   URL: {result['url']}")
        print(f"   Snippet: {result['snippet'][:150]}...")


```

---

#### ./example_usage.py:43

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    """Example: Ask a question"""
    print("\n" + "="*60)
    print("Example 3: Question Answering")
    print("="*60)
```

---

#### ./example_usage.py:44

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    print("\n" + "="*60)
    print("Example 3: Question Answering")
    print("="*60)
    
```

---

#### ./example_usage.py:45

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    print("Example 3: Question Answering")
    print("="*60)
    
    agent = ResearchAgent()
```

---

#### ./example_usage.py:51

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    
    print(f"\n❓ Question: {question}")
    print(f"\n💡 Answer:\n{answer}")

```

---

#### ./example_usage.py:52

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    print(f"\n❓ Question: {question}")
    print(f"\n💡 Answer:\n{answer}")


```

---

#### ./example_usage.py:57

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    """Example: Fetch content from a URL"""
    print("\n" + "="*60)
    print("Example 4: Fetch Content")
    print("="*60)
```

---

#### ./example_usage.py:58

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    print("\n" + "="*60)
    print("Example 4: Fetch Content")
    print("="*60)
    
```

---

#### ./example_usage.py:59

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    print("Example 4: Fetch Content")
    print("="*60)
    
    agent = ResearchAgent()
```

---

#### ./example_usage.py:66

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    if content:
        print(f"\n📄 Fetched content from {url}")
        print(f"Content length: {len(content)} characters")
        print(f"Preview:\n{content[:500]}...")
```

---

#### ./example_usage.py:67

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
        print(f"\n📄 Fetched content from {url}")
        print(f"Content length: {len(content)} characters")
        print(f"Preview:\n{content[:500]}...")

```

---

#### ./example_usage.py:68

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
        print(f"Content length: {len(content)} characters")
        print(f"Preview:\n{content[:500]}...")


```

---

#### ./example_usage.py:73

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    """Example: View research history"""
    print("\n" + "="*60)
    print("Example 5: Research History")
    print("="*60)
```

---

#### ./example_usage.py:74

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    print("\n" + "="*60)
    print("Example 5: Research History")
    print("="*60)
    
```

---

#### ./example_usage.py:75

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    print("Example 5: Research History")
    print("="*60)
    
    agent = ResearchAgent()
```

---

#### ./example_usage.py:81

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    if history:
        print(f"\n📚 Found {len(history)} past research sessions:")
        for item in history:
            print(f"\n  Topic: {item['topic']}")
```

---

#### ./example_usage.py:83

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
        for item in history:
            print(f"\n  Topic: {item['topic']}")
            print(f"  Date: {item['timestamp']}")
            print(f"  Sources: {item['num_sources']}")
```

---

#### ./example_usage.py:84

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
            print(f"\n  Topic: {item['topic']}")
            print(f"  Date: {item['timestamp']}")
            print(f"  Sources: {item['num_sources']}")
    else:
```

---

#### ./example_usage.py:85

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
            print(f"  Date: {item['timestamp']}")
            print(f"  Sources: {item['num_sources']}")
    else:
        print("\n📚 No research history found yet.")
```

---

#### ./example_usage.py:87

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    else:
        print("\n📚 No research history found yet.")


```

---

#### ./example_usage.py:92

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    """Example: Use custom output directory"""
    print("\n" + "="*60)
    print("Example 6: Custom Output Directory")
    print("="*60)
```

---

#### ./example_usage.py:93

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    print("\n" + "="*60)
    print("Example 6: Custom Output Directory")
    print("="*60)
    
```

---

#### ./example_usage.py:94

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    print("Example 6: Custom Output Directory")
    print("="*60)
    
    agent = ResearchAgent(output_dir="my_research")
```

---

#### ./example_usage.py:99

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    
    print(f"\n✅ Research saved to: {agent.output_dir}")


```

---

#### ./example_usage.py:104

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    """Run all examples"""
    print("\n🤖 Research Agent - Example Usage\n")
    
    try:
```

---

#### ./example_usage.py:115

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
        
        print("\n" + "="*60)
        print("✅ All examples completed!")
        print("="*60)
```

---

#### ./example_usage.py:116

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
        print("\n" + "="*60)
        print("✅ All examples completed!")
        print("="*60)
        
```

---

#### ./example_usage.py:117

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
        print("✅ All examples completed!")
        print("="*60)
        
    except KeyboardInterrupt:
```

---

#### ./example_usage.py:120

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    except KeyboardInterrupt:
        print("\n\n⚠️ Examples interrupted by user")
    except Exception as e:
        print(f"\n❌ Error running examples: {str(e)}")
```

---

#### ./example_usage.py:122

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    except Exception as e:
        print(f"\n❌ Error running examples: {str(e)}")


```

---

#### ./agents/bug-bot/bug_bot.py:120

**Category:** null-check  
**Issue:** Accessing dict without None check  
**Confidence:** 80%  
**Fix:** Check if key exists or use .get() with default  

**Code:**
```python
                    (r'\.(?:get|pop)\([^)]*\)(?!.*if)', 'Accessing dict without None check',
                     'Check if key exists or use .get() with default'),
                    
                    (r'\[.*\](?!.*try|.*if.*len)', 'Index access without bounds check',
```

---

#### ./agents/bug-bot/bug_bot.py:238

**Category:** null-check  
**Issue:** Accessing dict without None check  
**Confidence:** 80%  
**Fix:** Check if key exists or use .get() with default  

**Code:**
```python
        
        language = language_map.get(ext)
        if not language:
            return bugs
```

---

#### ./agents/bug-bot/bug_bot.py:243

**Category:** null-check  
**Issue:** Accessing dict without None check  
**Confidence:** 80%  
**Fix:** Check if key exists or use .get() with default  

**Code:**
```python
        # Check patterns for this language
        for severity, patterns in self.bug_patterns.get(language, {}).items():
            for pattern in patterns:
                if len(pattern) == 2:
```

---

#### ./agents/bug-bot/bug_bot.py:26

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
    description: str
    suggestion: Optional[str] = None
    confidence: float = 0.8
    detected_at: str = None
```

---

#### ./agents/bug-bot/bug_bot.py:44

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
    low_bugs: int
    bugs_by_category: Dict[str, int]
    bugs_by_file: Dict[str, int]
    all_bugs: List[Bug]
```

---

#### ./agents/bug-bot/bug_bot.py:45

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
    bugs_by_category: Dict[str, int]
    bugs_by_file: Dict[str, int]
    all_bugs: List[Bug]
    summary: str
```

---

#### ./agents/bug-bot/bug_bot.py:46

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
    bugs_by_file: Dict[str, int]
    all_bugs: List[Bug]
    summary: str

```

---

#### ./agents/bug-bot/bug_bot.py:64

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
                    # Security vulnerabilities
                    (r'eval\([^)]*\)', 'Code injection vulnerability with eval()', 
                     'Avoid eval() - use ast.literal_eval() for safe evaluation'),
                    
```

---

#### ./agents/bug-bot/bug_bot.py:67

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
                    
                    (r'exec\([^)]*\)', 'Code execution vulnerability with exec()',
                     'Avoid exec() - refactor to use safe alternatives'),
                    
```

---

#### ./agents/bug-bot/bug_bot.py:70

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
                    
                    (r'pickle\.loads?\([^)]*\)', 'Arbitrary code execution with pickle',
                     'Use json instead of pickle for untrusted data'),
                    
```

---

#### ./agents/bug-bot/bug_bot.py:76

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
                    
                    (r'PASSWORD\s*=\s*["\'][^"\']+["\']', 'Hardcoded password',
                     'Use environment variables or secure vault'),
                    
```

---

#### ./agents/bug-bot/bug_bot.py:79

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
                    
                    (r'API[_-]?KEY\s*=\s*["\'][^"\']+["\']', 'Hardcoded API key',
                     'Use environment variables for API keys'),
                    
```

---

#### ./agents/bug-bot/bug_bot.py:83

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
                    # SQL Injection
                    (r'execute\(["\'].*%s.*["\']', 'Potential SQL injection',
                     'Use parameterized queries instead of string formatting'),
                    
```

---

#### ./agents/bug-bot/bug_bot.py:102

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
                    # Resource leaks
                    (r'open\([^)]+\)(?!.*with)', 'File not closed - resource leak',
                     'Use "with open(...) as f:" to ensure file is closed'),
                    
```

---

#### ./agents/bug-bot/bug_bot.py:119

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
                    # Null/None issues
                    (r'\.(?:get|pop)\([^)]*\)(?!.*if)', 'Accessing dict without None check',
                     'Check if key exists or use .get() with default'),
                    
```

---

#### ./agents/bug-bot/bug_bot.py:133

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
                    # Mutable defaults
                    (r'def\s+\w+\([^)]*=\s*\[\]', 'Mutable default argument []',
                     'Use None as default and create list inside function'),
                    
```

---

#### ./agents/bug-bot/bug_bot.py:136

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
                    
                    (r'def\s+\w+\([^)]*=\s*\{\}', 'Mutable default argument {}',
                     'Use None as default and create dict inside function'),
                ],
```

---

#### ./agents/bug-bot/bug_bot.py:181

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
                    
                    (r'setTimeout\([^,]+,\s*0\)', 'setTimeout with 0 delay',
                     'Consider using requestAnimationFrame or microtask'),
                ],
```

---

#### ./agents/bug-bot/bug_bot.py:217

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
    
    def scan_file(self, file_path: str) -> List[Bug]:
        """Scan a single file for bugs."""
        bugs = []
```

---

#### ./agents/bug-bot/bug_bot.py:219

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        """Scan a single file for bugs."""
        bugs = []
        
        try:
```

---

#### ./agents/bug-bot/bug_bot.py:229

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        # Determine language
        ext = os.path.splitext(file_path)[1]
        language_map = {
            '.py': 'python',
```

---

#### ./agents/bug-bot/bug_bot.py:255

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
                for match in matches:
                    line_num = content[:match.start()].count('\n') + 1
                    
                    # Get code snippet (context)
```

---

#### ./agents/bug-bot/bug_bot.py:261

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
                    snippet_end = min(len(lines), line_num + 2)
                    snippet = '\n'.join(lines[snippet_start:snippet_end])
                    
                    # Determine category from description
```

---

#### ./agents/bug-bot/bug_bot.py:292

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
    
    def scan_directory(self, directory: str = None, exclude_patterns: List[str] = None) -> List[Bug]:
        """Scan entire directory for bugs."""
        if directory is None:
```

---

#### ./agents/bug-bot/bug_bot.py:312

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        
        all_bugs = []
        file_count = 0
        
```

---

#### ./agents/bug-bot/bug_bot.py:319

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
            # Filter out excluded directories
            dirs[:] = [d for d in dirs if not any(ex in d for ex in exclude_patterns)]
            
            for file in files:
```

---

#### ./agents/bug-bot/bug_bot.py:339

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        # Update history
        self.bug_history['scans_performed'] += 1
        self.bug_history['total_bugs_found'] += len(all_bugs)
        self._save_bug_history()
```

---

#### ./agents/bug-bot/bug_bot.py:340

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        self.bug_history['scans_performed'] += 1
        self.bug_history['total_bugs_found'] += len(all_bugs)
        self._save_bug_history()
        
```

---

#### ./agents/bug-bot/bug_bot.py:345

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
    
    def generate_report(self, bugs: List[Bug]) -> BugReport:
        """Generate comprehensive bug report."""
        # Count by severity
```

---

#### ./agents/bug-bot/bug_bot.py:358

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        for bug in bugs:
            bugs_by_category[bug.category] += 1
        
        # Count by file
```

---

#### ./agents/bug-bot/bug_bot.py:363

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        for bug in bugs:
            bugs_by_file[bug.file_path] += 1
        
        # Generate summary
```

---

#### ./agents/bug-bot/bug_bot.py:368

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
            summary = "✅ Excellent! No bugs detected."
        elif severity_counts['critical'] > 0:
            summary = f"🚨 CRITICAL: {severity_counts['critical']} critical bug(s) found - immediate action required!"
        elif severity_counts['high'] > 0:
```

---

#### ./agents/bug-bot/bug_bot.py:369

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        elif severity_counts['critical'] > 0:
            summary = f"🚨 CRITICAL: {severity_counts['critical']} critical bug(s) found - immediate action required!"
        elif severity_counts['high'] > 0:
            summary = f"⚠️  HIGH: {severity_counts['high']} high-severity bug(s) found - should fix soon."
```

---

#### ./agents/bug-bot/bug_bot.py:370

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
            summary = f"🚨 CRITICAL: {severity_counts['critical']} critical bug(s) found - immediate action required!"
        elif severity_counts['high'] > 0:
            summary = f"⚠️  HIGH: {severity_counts['high']} high-severity bug(s) found - should fix soon."
        else:
```

---

#### ./agents/bug-bot/bug_bot.py:371

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        elif severity_counts['high'] > 0:
            summary = f"⚠️  HIGH: {severity_counts['high']} high-severity bug(s) found - should fix soon."
        else:
            summary = f"💡 Found {len(bugs)} potential issue(s) - mostly minor."
```

---

#### ./agents/bug-bot/bug_bot.py:378

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
            total_bugs=len(bugs),
            critical_bugs=severity_counts['critical'],
            high_bugs=severity_counts['high'],
            medium_bugs=severity_counts['medium'],
```

---

#### ./agents/bug-bot/bug_bot.py:379

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
            critical_bugs=severity_counts['critical'],
            high_bugs=severity_counts['high'],
            medium_bugs=severity_counts['medium'],
            low_bugs=severity_counts['low'],
```

---

#### ./agents/bug-bot/bug_bot.py:380

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
            high_bugs=severity_counts['high'],
            medium_bugs=severity_counts['medium'],
            low_bugs=severity_counts['low'],
            bugs_by_category=dict(bugs_by_category),
```

---

#### ./agents/bug-bot/bug_bot.py:381

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
            medium_bugs=severity_counts['medium'],
            low_bugs=severity_counts['low'],
            bugs_by_category=dict(bugs_by_category),
            bugs_by_file=dict(bugs_by_file),
```

---

#### ./agents/bug-bot/bug_bot.py:410

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
                'summary': report.summary,
                'bugs': [asdict(b) for b in report.all_bugs]
            }
            json.dump(report_dict, f, indent=2)
```

---

#### ./agents/bug-bot/bug_bot.py:445

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
"""
        for category, count in sorted(report.bugs_by_category.items(), key=lambda x: -x[1]):
            md += f"- **{category.title()}:** {count}\n"
        
```

---

#### ./agents/bug-bot/bug_bot.py:449

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        md += "\n## Files with Most Bugs\n\n"
        top_files = sorted(report.bugs_by_file.items(), key=lambda x: -x[1])[:10]
        for file_path, count in top_files:
            md += f"- `{file_path}`: {count} bug(s)\n"
```

---

#### ./agents/bug-bot/bug_bot.py:457

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
            # Group by severity
            for severity in ['critical', 'high', 'medium', 'low']:
                bugs_in_severity = [b for b in report.all_bugs if b.severity == severity]
                if not bugs_in_severity:
```

---

#### ./agents/bug-bot/bug_bot.py:458

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
            for severity in ['critical', 'high', 'medium', 'low']:
                bugs_in_severity = [b for b in report.all_bugs if b.severity == severity]
                if not bugs_in_severity:
                    continue
```

---

#### ./agents/bug-bot/bug_bot.py:469

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
                
                md += f"\n### {severity_emoji[severity]} {severity.upper()} ({len(bugs_in_severity)})\n\n"
                
                for bug in bugs_in_severity:
```

---

#### ./agents/bug-bot/bug_bot.py:498

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
    if len(sys.argv) > 1:
        target = sys.argv[1]
    else:
        target = "."
```

---

#### ./agents/bug-bot/bug_bot.py:225

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
        except Exception as e:
            print(f"⚠️  Could not read {file_path}: {e}")
            return bugs
        
```

---

#### ./agents/bug-bot/bug_bot.py:315

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
        
        print(f"\n🔍 Scanning {directory} for bugs...")
        
        for root, dirs, files in os.walk(directory):
```

---

#### ./agents/bug-bot/bug_bot.py:332

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
                    file_count += 1
                    print(f"  📄 {rel_path}")
                    bugs = self.scan_file(file_path)
                    all_bugs.extend(bugs)
```

---

#### ./agents/bug-bot/bug_bot.py:336

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
        
        print(f"\n✅ Scanned {file_count} files, found {len(all_bugs)} potential bugs")
        
        # Update history
```

---

#### ./agents/bug-bot/bug_bot.py:414

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
        
        print(f"💾 JSON report saved to: {json_file}")
        
        # Save Markdown
```

---

#### ./agents/bug-bot/bug_bot.py:488

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
        
        print(f"📄 Markdown report saved to: {filepath}")


```

---

#### ./agents/bug-bot/bug_bot.py:509

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    # Display summary
    print(f"\n{'='*60}")
    print(report.summary)
    print(f"{'='*60}")
```

---

#### ./agents/bug-bot/bug_bot.py:510

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    print(f"\n{'='*60}")
    print(report.summary)
    print(f"{'='*60}")
    print(f"\nTotal: {report.total_bugs} bugs")
```

---

#### ./agents/bug-bot/bug_bot.py:511

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    print(report.summary)
    print(f"{'='*60}")
    print(f"\nTotal: {report.total_bugs} bugs")
    print(f"  🚨 Critical: {report.critical_bugs}")
```

---

#### ./agents/bug-bot/bug_bot.py:512

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    print(f"{'='*60}")
    print(f"\nTotal: {report.total_bugs} bugs")
    print(f"  🚨 Critical: {report.critical_bugs}")
    print(f"  ⚠️  High: {report.high_bugs}")
```

---

#### ./agents/bug-bot/bug_bot.py:513

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    print(f"\nTotal: {report.total_bugs} bugs")
    print(f"  🚨 Critical: {report.critical_bugs}")
    print(f"  ⚠️  High: {report.high_bugs}")
    print(f"  💡 Medium: {report.medium_bugs}")
```

---

#### ./agents/bug-bot/bug_bot.py:514

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    print(f"  🚨 Critical: {report.critical_bugs}")
    print(f"  ⚠️  High: {report.high_bugs}")
    print(f"  💡 Medium: {report.medium_bugs}")
    print(f"  🔸 Low: {report.low_bugs}")
```

---

#### ./agents/bug-bot/bug_bot.py:515

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    print(f"  ⚠️  High: {report.high_bugs}")
    print(f"  💡 Medium: {report.medium_bugs}")
    print(f"  🔸 Low: {report.low_bugs}")
    
```

---

#### ./agents/bug-bot/bug_bot.py:516

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    print(f"  💡 Medium: {report.medium_bugs}")
    print(f"  🔸 Low: {report.low_bugs}")
    
    # Save report
```

---

#### ./agents/pr-review-bot/review_bot.py:96

**Category:** null-check  
**Issue:** Accessing dict without None check  
**Confidence:** 80%  
**Fix:** Check if key exists or use .get() with default  

**Code:**
```python
        # Adjust based on category acceptance rate
        category_stats = self.feedback_history['comment_acceptance_rate'].get(comment.category)
        if category_stats and category_stats['total'] > 5:
            acceptance_rate = category_stats['accepted'] / category_stats['total']
```

---

#### ./agents/pr-review-bot/review_bot.py:102

**Category:** null-check  
**Issue:** Accessing dict without None check  
**Confidence:** 80%  
**Fix:** Check if key exists or use .get() with default  

**Code:**
```python
        # Adjust based on severity accuracy
        severity_stats = self.feedback_history['severity_accuracy'].get(comment.severity)
        if severity_stats and severity_stats['total'] > 5:
            accuracy = severity_stats['correct'] / severity_stats['total']
```

---

#### ./agents/pr-review-bot/review_bot.py:243

**Category:** null-check  
**Issue:** Accessing dict without None check  
**Confidence:** 80%  
**Fix:** Check if key exists or use .get() with default  

**Code:**
```python
        # Check general patterns
        for category, patterns in self.review_patterns.get('general', {}).items():
            for pattern, message in patterns:
                matches = re.finditer(pattern, content, re.MULTILINE)
```

---

#### ./agents/pr-review-bot/review_bot.py:266

**Category:** null-check  
**Issue:** Accessing dict without None check  
**Confidence:** 80%  
**Fix:** Check if key exists or use .get() with default  

**Code:**
```python
        # Check PR title
        title = pr_info.get('title', '')
        if len(title) < 10:
            comments.append(ReviewComment(
```

---

#### ./agents/pr-review-bot/review_bot.py:278

**Category:** null-check  
**Issue:** Accessing dict without None check  
**Confidence:** 80%  
**Fix:** Check if key exists or use .get() with default  

**Code:**
```python
        # Check PR body
        body = pr_info.get('body', '')
        if len(body) < 50:
            comments.append(ReviewComment(
```

---

#### ./agents/pr-review-bot/review_bot.py:290

**Category:** null-check  
**Issue:** Accessing dict without None check  
**Confidence:** 80%  
**Fix:** Check if key exists or use .get() with default  

**Code:**
```python
        # Check file count
        files = pr_info.get('files', [])
        if len(files) > 20:
            comments.append(ReviewComment(
```

---

#### ./agents/pr-review-bot/review_bot.py:302

**Category:** null-check  
**Issue:** Accessing dict without None check  
**Confidence:** 80%  
**Fix:** Check if key exists or use .get() with default  

**Code:**
```python
        # Check additions/deletions
        additions = pr_info.get('additions', 0)
        deletions = pr_info.get('deletions', 0)
        total_changes = additions + deletions
```

---

#### ./agents/pr-review-bot/review_bot.py:303

**Category:** null-check  
**Issue:** Accessing dict without None check  
**Confidence:** 80%  
**Fix:** Check if key exists or use .get() with default  

**Code:**
```python
        additions = pr_info.get('additions', 0)
        deletions = pr_info.get('deletions', 0)
        total_changes = additions + deletions
        
```

---

#### ./agents/pr-review-bot/review_bot.py:333

**Category:** null-check  
**Issue:** Accessing dict without None check  
**Confidence:** 80%  
**Fix:** Check if key exists or use .get() with default  

**Code:**
```python
        for comment in comments:
            weight = severity_weights.get(comment.severity, -0.5)
            # Weight by confidence
            total_deduction += abs(weight) * comment.confidence
```

---

#### ./agents/pr-review-bot/review_bot.py:347

**Category:** null-check  
**Issue:** Accessing dict without None check  
**Confidence:** 80%  
**Fix:** Check if key exists or use .get() with default  

**Code:**
```python
        for comment in comments:
            severity_counts[comment.severity] = severity_counts.get(comment.severity, 0) + 1
        
        # Generate summary
```

---

#### ./agents/pr-review-bot/review_bot.py:361

**Category:** null-check  
**Issue:** Accessing dict without None check  
**Confidence:** 80%  
**Fix:** Check if key exists or use .get() with default  

**Code:**
```python
        strengths = []
        additions = pr_info.get('additions', 0)
        
        if additions > 0:
```

---

#### ./agents/pr-review-bot/review_bot.py:366

**Category:** null-check  
**Issue:** Accessing dict without None check  
**Confidence:** 80%  
**Fix:** Check if key exists or use .get() with default  

**Code:**
```python
        
        if pr_info.get('body', '') and len(pr_info.get('body', '')) > 100:
            strengths.append("Well-documented PR with good description")
        
```

---

#### ./agents/pr-review-bot/review_bot.py:366

**Category:** null-check  
**Issue:** Accessing dict without None check  
**Confidence:** 80%  
**Fix:** Check if key exists or use .get() with default  

**Code:**
```python
        
        if pr_info.get('body', '') and len(pr_info.get('body', '')) > 100:
            strengths.append("Well-documented PR with good description")
        
```

---

#### ./agents/pr-review-bot/review_bot.py:369

**Category:** null-check  
**Issue:** Accessing dict without None check  
**Confidence:** 80%  
**Fix:** Check if key exists or use .get() with default  

**Code:**
```python
        
        files = pr_info.get('files', [])
        if len(files) < 10:
            strengths.append("Focused PR with manageable scope")
```

---

#### ./agents/pr-review-bot/review_bot.py:390

**Category:** null-check  
**Issue:** Accessing dict without None check  
**Confidence:** 80%  
**Fix:** Check if key exists or use .get() with default  

**Code:**
```python
        for comment in comments:
            category_counts[comment.category] = category_counts.get(comment.category, 0) + 1
        
        if category_counts.get('documentation', 0) > 2:
```

---

#### ./agents/pr-review-bot/review_bot.py:392

**Category:** null-check  
**Issue:** Accessing dict without None check  
**Confidence:** 80%  
**Fix:** Check if key exists or use .get() with default  

**Code:**
```python
        
        if category_counts.get('documentation', 0) > 2:
            improvements.append("Add more documentation (docstrings, comments)")
        
```

---

#### ./agents/pr-review-bot/review_bot.py:395

**Category:** null-check  
**Issue:** Accessing dict without None check  
**Confidence:** 80%  
**Fix:** Check if key exists or use .get() with default  

**Code:**
```python
        
        if category_counts.get('security', 0) > 0:
            improvements.append("Review and fix security concerns")
        
```

---

#### ./agents/pr-review-bot/review_bot.py:410

**Category:** null-check  
**Issue:** Accessing dict without None check  
**Confidence:** 80%  
**Fix:** Check if key exists or use .get() with default  

**Code:**
```python
        
        print(f"📝 Reviewing: {pr_info.get('title', 'Untitled PR')}")
        
        all_comments = []
```

---

#### ./agents/pr-review-bot/review_bot.py:420

**Category:** null-check  
**Issue:** Accessing dict without None check  
**Confidence:** 80%  
**Fix:** Check if key exists or use .get() with default  

**Code:**
```python
        # Analyze each file
        files = pr_info.get('files', [])
        print(f"📄 Analyzing {len(files)} file(s)...")
        
```

---

#### ./agents/pr-review-bot/review_bot.py:424

**Category:** null-check  
**Issue:** Accessing dict without None check  
**Confidence:** 80%  
**Fix:** Check if key exists or use .get() with default  

**Code:**
```python
        for file_info in files:
            file_path = file_info.get('path', '')
            print(f"  • {file_path}")
            
```

---

#### ./agents/pr-review-bot/review_bot.py:442

**Category:** null-check  
**Issue:** Accessing dict without None check  
**Confidence:** 80%  
**Fix:** Check if key exists or use .get() with default  

**Code:**
```python
            pr_number=pr_number,
            pr_title=pr_info.get('title', ''),
            branch=pr_info.get('headRefName', ''),
            timestamp=datetime.now().isoformat(),
```

---

#### ./agents/pr-review-bot/review_bot.py:443

**Category:** null-check  
**Issue:** Accessing dict without None check  
**Confidence:** 80%  
**Fix:** Check if key exists or use .get() with default  

**Code:**
```python
            pr_title=pr_info.get('title', ''),
            branch=pr_info.get('headRefName', ''),
            timestamp=datetime.now().isoformat(),
            comments=all_comments,
```

---

#### ./agents/pr-review-bot/review_bot.py:19

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
    file_path: str
    line_number: Optional[int]
    severity: str  # 'critical', 'major', 'minor', 'suggestion'
    category: str  # 'bug', 'style', 'performance', 'security', 'documentation', 'best-practice'
```

---

#### ./agents/pr-review-bot/review_bot.py:23

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
    message: str
    suggestion: Optional[str] = None
    confidence: float = 0.8  # 0.0 to 1.0

```

---

#### ./agents/pr-review-bot/review_bot.py:34

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
    timestamp: str
    comments: List[ReviewComment]
    overall_score: float  # 0.0 to 10.0
    summary: str
```

---

#### ./agents/pr-review-bot/review_bot.py:37

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
    summary: str
    strengths: List[str]
    areas_for_improvement: List[str]

```

---

#### ./agents/pr-review-bot/review_bot.py:38

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
    strengths: List[str]
    areas_for_improvement: List[str]


```

---

#### ./agents/pr-review-bot/review_bot.py:73

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        # Update acceptance rate for category
        if category not in self.feedback_history['comment_acceptance_rate']:
            self.feedback_history['comment_acceptance_rate'][category] = {'accepted': 0, 'total': 0}
        
```

---

#### ./agents/pr-review-bot/review_bot.py:74

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        if category not in self.feedback_history['comment_acceptance_rate']:
            self.feedback_history['comment_acceptance_rate'][category] = {'accepted': 0, 'total': 0}
        
        self.feedback_history['comment_acceptance_rate'][category]['total'] += 1
```

---

#### ./agents/pr-review-bot/review_bot.py:76

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        
        self.feedback_history['comment_acceptance_rate'][category]['total'] += 1
        if accepted:
            self.feedback_history['comment_acceptance_rate'][category]['accepted'] += 1
```

---

#### ./agents/pr-review-bot/review_bot.py:78

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        if accepted:
            self.feedback_history['comment_acceptance_rate'][category]['accepted'] += 1
        
        # Update severity accuracy
```

---

#### ./agents/pr-review-bot/review_bot.py:81

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        # Update severity accuracy
        if severity not in self.feedback_history['severity_accuracy']:
            self.feedback_history['severity_accuracy'][severity] = {'correct': 0, 'total': 0}
        
```

---

#### ./agents/pr-review-bot/review_bot.py:82

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        if severity not in self.feedback_history['severity_accuracy']:
            self.feedback_history['severity_accuracy'][severity] = {'correct': 0, 'total': 0}
        
        self.feedback_history['severity_accuracy'][severity]['total'] += 1
```

---

#### ./agents/pr-review-bot/review_bot.py:84

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        
        self.feedback_history['severity_accuracy'][severity]['total'] += 1
        if accepted and comment.confidence > 0.7:  # High confidence and accepted
            self.feedback_history['severity_accuracy'][severity]['correct'] += 1
```

---

#### ./agents/pr-review-bot/review_bot.py:86

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        if accepted and comment.confidence > 0.7:  # High confidence and accepted
            self.feedback_history['severity_accuracy'][severity]['correct'] += 1
        
        self.feedback_history['feedback_count'] += 1
```

---

#### ./agents/pr-review-bot/review_bot.py:88

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        
        self.feedback_history['feedback_count'] += 1
        self._save_feedback()
    
```

---

#### ./agents/pr-review-bot/review_bot.py:96

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        # Adjust based on category acceptance rate
        category_stats = self.feedback_history['comment_acceptance_rate'].get(comment.category)
        if category_stats and category_stats['total'] > 5:
            acceptance_rate = category_stats['accepted'] / category_stats['total']
```

---

#### ./agents/pr-review-bot/review_bot.py:97

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        category_stats = self.feedback_history['comment_acceptance_rate'].get(comment.category)
        if category_stats and category_stats['total'] > 5:
            acceptance_rate = category_stats['accepted'] / category_stats['total']
            base_confidence *= (0.7 + 0.3 * acceptance_rate)  # Weight by acceptance
```

---

#### ./agents/pr-review-bot/review_bot.py:98

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        if category_stats and category_stats['total'] > 5:
            acceptance_rate = category_stats['accepted'] / category_stats['total']
            base_confidence *= (0.7 + 0.3 * acceptance_rate)  # Weight by acceptance
        
```

---

#### ./agents/pr-review-bot/review_bot.py:102

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        # Adjust based on severity accuracy
        severity_stats = self.feedback_history['severity_accuracy'].get(comment.severity)
        if severity_stats and severity_stats['total'] > 5:
            accuracy = severity_stats['correct'] / severity_stats['total']
```

---

#### ./agents/pr-review-bot/review_bot.py:103

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        severity_stats = self.feedback_history['severity_accuracy'].get(comment.severity)
        if severity_stats and severity_stats['total'] > 5:
            accuracy = severity_stats['correct'] / severity_stats['total']
            base_confidence *= (0.7 + 0.3 * accuracy)
```

---

#### ./agents/pr-review-bot/review_bot.py:104

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        if severity_stats and severity_stats['total'] > 5:
            accuracy = severity_stats['correct'] / severity_stats['total']
            base_confidence *= (0.7 + 0.3 * accuracy)
        
```

---

#### ./agents/pr-review-bot/review_bot.py:112

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        stats = {
            'total_reviews': self.feedback_history['total_reviews'],
            'feedback_count': self.feedback_history['feedback_count'],
            'category_performance': {},
```

---

#### ./agents/pr-review-bot/review_bot.py:113

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
            'total_reviews': self.feedback_history['total_reviews'],
            'feedback_count': self.feedback_history['feedback_count'],
            'category_performance': {},
            'severity_accuracy': {}
```

---

#### ./agents/pr-review-bot/review_bot.py:118

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        
        for category, data in self.feedback_history['comment_acceptance_rate'].items():
            if data['total'] > 0:
                stats['category_performance'][category] = {
```

---

#### ./agents/pr-review-bot/review_bot.py:119

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        for category, data in self.feedback_history['comment_acceptance_rate'].items():
            if data['total'] > 0:
                stats['category_performance'][category] = {
                    'acceptance_rate': data['accepted'] / data['total'],
```

---

#### ./agents/pr-review-bot/review_bot.py:120

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
            if data['total'] > 0:
                stats['category_performance'][category] = {
                    'acceptance_rate': data['accepted'] / data['total'],
                    'sample_size': data['total']
```

---

#### ./agents/pr-review-bot/review_bot.py:121

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
                stats['category_performance'][category] = {
                    'acceptance_rate': data['accepted'] / data['total'],
                    'sample_size': data['total']
                }
```

---

#### ./agents/pr-review-bot/review_bot.py:122

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
                    'acceptance_rate': data['accepted'] / data['total'],
                    'sample_size': data['total']
                }
        
```

---

#### ./agents/pr-review-bot/review_bot.py:125

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        
        for severity, data in self.feedback_history['severity_accuracy'].items():
            if data['total'] > 0:
                stats['severity_accuracy'][severity] = {
```

---

#### ./agents/pr-review-bot/review_bot.py:126

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        for severity, data in self.feedback_history['severity_accuracy'].items():
            if data['total'] > 0:
                stats['severity_accuracy'][severity] = {
                    'accuracy': data['correct'] / data['total'],
```

---

#### ./agents/pr-review-bot/review_bot.py:127

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
            if data['total'] > 0:
                stats['severity_accuracy'][severity] = {
                    'accuracy': data['correct'] / data['total'],
                    'sample_size': data['total']
```

---

#### ./agents/pr-review-bot/review_bot.py:128

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
                stats['severity_accuracy'][severity] = {
                    'accuracy': data['correct'] / data['total'],
                    'sample_size': data['total']
                }
```

---

#### ./agents/pr-review-bot/review_bot.py:129

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
                    'accuracy': data['correct'] / data['total'],
                    'sample_size': data['total']
                }
        
```

---

#### ./agents/pr-review-bot/review_bot.py:174

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
                'documentation': [
                    (r'^[^#\n]*def\s+\w+.*:\s*$', 'Missing docstring for function'),
                    (r'^[^#\n]*class\s+\w+.*:\s*$', 'Missing docstring for class'),
                ],
```

---

#### ./agents/pr-review-bot/review_bot.py:175

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
                    (r'^[^#\n]*def\s+\w+.*:\s*$', 'Missing docstring for function'),
                    (r'^[^#\n]*class\s+\w+.*:\s*$', 'Missing docstring for class'),
                ],
                'best-practice': [
```

---

#### ./agents/pr-review-bot/review_bot.py:183

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
    
    def get_pr_info(self, pr_number: int) -> Optional[Dict]:
        """Get PR information using gh CLI."""
        try:
```

---

#### ./agents/pr-review-bot/review_bot.py:202

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
            result = subprocess.run(
                ['gh', 'pr', 'diff', str(pr_number)],
                capture_output=True, text=True, cwd=self.repo_path
            )
```

---

#### ./agents/pr-review-bot/review_bot.py:211

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
    
    def analyze_code_patterns(self, content: str, file_path: str) -> List[ReviewComment]:
        """Analyze code for patterns and issues."""
        comments = []
```

---

#### ./agents/pr-review-bot/review_bot.py:213

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        """Analyze code for patterns and issues."""
        comments = []
        
        # Determine file type
```

---

#### ./agents/pr-review-bot/review_bot.py:216

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        # Determine file type
        file_ext = os.path.splitext(file_path)[1]
        language = None
        if file_ext == '.py':
```

---

#### ./agents/pr-review-bot/review_bot.py:220

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
            language = 'python'
        elif file_ext in ['.js', '.jsx', '.ts', '.tsx']:
            language = 'javascript'
        
```

---

#### ./agents/pr-review-bot/review_bot.py:225

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        if language and language in self.review_patterns:
            for severity, patterns in self.review_patterns[language].items():
                for pattern, message in patterns:
                    matches = re.finditer(pattern, content, re.MULTILINE)
```

---

#### ./agents/pr-review-bot/review_bot.py:229

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
                    for match in matches:
                        line_num = content[:match.start()].count('\n') + 1
                        comment = ReviewComment(
                            file_path=file_path,
```

---

#### ./agents/pr-review-bot/review_bot.py:247

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
                for match in matches:
                    line_num = content[:match.start()].count('\n') + 1
                    comment = ReviewComment(
                        file_path=file_path,
```

---

#### ./agents/pr-review-bot/review_bot.py:261

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
    
    def analyze_structure(self, pr_info: Dict) -> List[ReviewComment]:
        """Analyze PR structure and metadata."""
        comments = []
```

---

#### ./agents/pr-review-bot/review_bot.py:263

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        """Analyze PR structure and metadata."""
        comments = []
        
        # Check PR title
```

---

#### ./agents/pr-review-bot/review_bot.py:290

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        # Check file count
        files = pr_info.get('files', [])
        if len(files) > 20:
            comments.append(ReviewComment(
```

---

#### ./agents/pr-review-bot/review_bot.py:318

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
    
    def calculate_overall_score(self, comments: List[ReviewComment]) -> float:
        """Calculate overall PR quality score (0-10)."""
        if not comments:
```

---

#### ./agents/pr-review-bot/review_bot.py:341

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
    
    def generate_summary(self, comments: List[ReviewComment], pr_info: Dict) -> Tuple[str, List[str], List[str]]:
        """Generate review summary with strengths and areas for improvement."""
        
```

---

#### ./agents/pr-review-bot/review_bot.py:347

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        for comment in comments:
            severity_counts[comment.severity] = severity_counts.get(comment.severity, 0) + 1
        
        # Generate summary
```

---

#### ./agents/pr-review-bot/review_bot.py:352

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
            summary = "✅ Excellent! No issues found. Code looks clean and well-structured."
        elif severity_counts['critical'] > 0:
            summary = f"⚠️ Found {severity_counts['critical']} critical issue(s) that need immediate attention."
        elif severity_counts['major'] > 0:
```

---

#### ./agents/pr-review-bot/review_bot.py:353

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        elif severity_counts['critical'] > 0:
            summary = f"⚠️ Found {severity_counts['critical']} critical issue(s) that need immediate attention."
        elif severity_counts['major'] > 0:
            summary = f"⚡ Found {severity_counts['major']} major issue(s) to address before merging."
```

---

#### ./agents/pr-review-bot/review_bot.py:354

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
            summary = f"⚠️ Found {severity_counts['critical']} critical issue(s) that need immediate attention."
        elif severity_counts['major'] > 0:
            summary = f"⚡ Found {severity_counts['major']} major issue(s) to address before merging."
        else:
```

---

#### ./agents/pr-review-bot/review_bot.py:355

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        elif severity_counts['major'] > 0:
            summary = f"⚡ Found {severity_counts['major']} major issue(s) to address before merging."
        else:
            summary = f"✓ Generally good! Found {severity_counts['minor']} minor improvement(s)."
```

---

#### ./agents/pr-review-bot/review_bot.py:357

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        else:
            summary = f"✓ Generally good! Found {severity_counts['minor']} minor improvement(s)."
        
        # Identify strengths
```

---

#### ./agents/pr-review-bot/review_bot.py:360

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        # Identify strengths
        strengths = []
        additions = pr_info.get('additions', 0)
        
```

---

#### ./agents/pr-review-bot/review_bot.py:369

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        
        files = pr_info.get('files', [])
        if len(files) < 10:
            strengths.append("Focused PR with manageable scope")
```

---

#### ./agents/pr-review-bot/review_bot.py:373

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        
        if not comments or all(c.severity in ['minor', 'suggestion'] for c in comments):
            strengths.append("Clean code with good practices")
        
```

---

#### ./agents/pr-review-bot/review_bot.py:377

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        # Areas for improvement
        improvements = []
        
        if severity_counts['critical'] > 0:
```

---

#### ./agents/pr-review-bot/review_bot.py:379

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        
        if severity_counts['critical'] > 0:
            improvements.append(f"Address {severity_counts['critical']} critical security/bug issue(s)")
        
```

---

#### ./agents/pr-review-bot/review_bot.py:380

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        if severity_counts['critical'] > 0:
            improvements.append(f"Address {severity_counts['critical']} critical security/bug issue(s)")
        
        if severity_counts['major'] > 0:
```

---

#### ./agents/pr-review-bot/review_bot.py:382

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        
        if severity_counts['major'] > 0:
            improvements.append(f"Fix {severity_counts['major']} major issue(s)")
        
```

---

#### ./agents/pr-review-bot/review_bot.py:383

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        if severity_counts['major'] > 0:
            improvements.append(f"Fix {severity_counts['major']} major issue(s)")
        
        if severity_counts['minor'] > 3:
```

---

#### ./agents/pr-review-bot/review_bot.py:385

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        
        if severity_counts['minor'] > 3:
            improvements.append("Several minor style/documentation improvements possible")
        
```

---

#### ./agents/pr-review-bot/review_bot.py:390

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        for comment in comments:
            category_counts[comment.category] = category_counts.get(comment.category, 0) + 1
        
        if category_counts.get('documentation', 0) > 2:
```

---

#### ./agents/pr-review-bot/review_bot.py:400

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
    
    def review_pr(self, pr_number: int) -> Optional[ReviewResult]:
        """Perform complete review of a PR."""
        print(f"\n🤖 Starting AI review of PR #{pr_number}...")
```

---

#### ./agents/pr-review-bot/review_bot.py:412

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        
        all_comments = []
        
        # Analyze structure
```

---

#### ./agents/pr-review-bot/review_bot.py:420

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        # Analyze each file
        files = pr_info.get('files', [])
        print(f"📄 Analyzing {len(files)} file(s)...")
        
```

---

#### ./agents/pr-review-bot/review_bot.py:453

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        # Update learning
        self.learner.feedback_history['total_reviews'] += 1
        self.learner._save_feedback()
        
```

---

#### ./agents/pr-review-bot/review_bot.py:477

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
            'areas_for_improvement': result.areas_for_improvement,
            'comments': [asdict(c) for c in result.comments]
        }
        
```

---

#### ./agents/pr-review-bot/review_bot.py:522

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
            # Group by severity
            by_severity = {'critical': [], 'major': [], 'minor': [], 'suggestion': []}
            for comment in result.comments:
                by_severity[comment.severity].append(comment)
```

---

#### ./agents/pr-review-bot/review_bot.py:524

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
            for comment in result.comments:
                by_severity[comment.severity].append(comment)
            
            for severity in ['critical', 'major', 'minor', 'suggestion']:
```

---

#### ./agents/pr-review-bot/review_bot.py:526

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
            
            for severity in ['critical', 'major', 'minor', 'suggestion']:
                comments = by_severity[severity]
                if not comments:
```

---

#### ./agents/pr-review-bot/review_bot.py:527

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
            for severity in ['critical', 'major', 'minor', 'suggestion']:
                comments = by_severity[severity]
                if not comments:
                    continue
```

---

#### ./agents/pr-review-bot/review_bot.py:538

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
                
                report += f"\n### {severity_emoji[severity]} {severity.upper()} ({len(comments)})\n\n"
                
                for comment in comments:
```

---

#### ./agents/pr-review-bot/review_bot.py:569

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
    
    if sys.argv[1] == 'all':
        # Get all open PRs
        result = subprocess.run(
```

---

#### ./agents/pr-review-bot/review_bot.py:572

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
        result = subprocess.run(
            ['gh', 'pr', 'list', '--json', 'number'],
            capture_output=True, text=True
        )
```

---

#### ./agents/pr-review-bot/review_bot.py:578

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
            for pr in prs:
                pr_number = pr['number']
                review_result = bot.review_pr(pr_number)
                if review_result:
```

---

#### ./agents/pr-review-bot/review_bot.py:586

**Category:** logic  
**Issue:** Index access without bounds check  
**Confidence:** 80%  
**Fix:** Check list length before accessing index  

**Code:**
```python
    else:
        pr_number = int(sys.argv[1])
        review_result = bot.review_pr(pr_number)
        if review_result:
```

---

#### ./agents/pr-review-bot/review_bot.py:195

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
        except Exception as e:
            print(f"Error getting PR info: {e}")
            return None
    
```

---

#### ./agents/pr-review-bot/review_bot.py:402

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
        """Perform complete review of a PR."""
        print(f"\n🤖 Starting AI review of PR #{pr_number}...")
        
        # Get PR info
```

---

#### ./agents/pr-review-bot/review_bot.py:407

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
        if not pr_info:
            print(f"❌ Could not fetch PR #{pr_number}")
            return None
        
```

---

#### ./agents/pr-review-bot/review_bot.py:410

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
        
        print(f"📝 Reviewing: {pr_info.get('title', 'Untitled PR')}")
        
        all_comments = []
```

---

#### ./agents/pr-review-bot/review_bot.py:415

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
        # Analyze structure
        print("🔍 Analyzing PR structure...")
        structure_comments = self.analyze_structure(pr_info)
        all_comments.extend(structure_comments)
```

---

#### ./agents/pr-review-bot/review_bot.py:421

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
        files = pr_info.get('files', [])
        print(f"📄 Analyzing {len(files)} file(s)...")
        
        for file_info in files:
```

---

#### ./agents/pr-review-bot/review_bot.py:425

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
            file_path = file_info.get('path', '')
            print(f"  • {file_path}")
            
            # Read file content
```

---

#### ./agents/pr-review-bot/review_bot.py:434

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
            except Exception as e:
                print(f"    ⚠️ Could not read file: {e}")
        
        # Calculate score and generate summary
```

---

#### ./agents/pr-review-bot/review_bot.py:456

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
        
        print(f"\n✅ Review complete! Overall score: {score}/10")
        return result
    
```

---

#### ./agents/pr-review-bot/review_bot.py:483

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
        
        print(f"💾 Review saved to: {filepath}")
        
        # Also create markdown report
```

---

#### ./agents/pr-review-bot/review_bot.py:555

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
        
        print(f"📄 Markdown report saved to: {filepath}")


```

---

#### ./agents/pr-review-bot/review_bot.py:563

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    if len(sys.argv) < 2:
        print("Usage: python review_bot.py <pr_number>")
        print("   or: python review_bot.py all  (review all open PRs)")
        sys.exit(1)
```

---

#### ./agents/pr-review-bot/review_bot.py:564

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
        print("Usage: python review_bot.py <pr_number>")
        print("   or: python review_bot.py all  (review all open PRs)")
        sys.exit(1)
    
```

---

#### ./agents/pr-review-bot/review_bot.py:582

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
                    bot.save_review(review_result)
                    print()
        else:
            print("❌ Failed to fetch PRs")
```

---

#### ./agents/pr-review-bot/review_bot.py:584

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
        else:
            print("❌ Failed to fetch PRs")
    else:
        pr_number = int(sys.argv[1])
```

---

#### ./agents/pr-review-bot/review_bot.py:586

**Category:** type-error  
**Issue:** Type conversion without error handling  
**Confidence:** 80%  
**Fix:** Wrap in try-except to handle ValueError  

**Code:**
```python
    else:
        pr_number = int(sys.argv[1])
        review_result = bot.review_pr(pr_number)
        if review_result:
```

---


### 🔸 LOW (10)

#### ./agents/bug-bot/bug_bot.py:149

**Category:** logic  
**Issue:** Unresolved TODO/FIXME comment  
**Confidence:** 80%  
**Fix:** Address or create an issue to track  

**Code:**
```python
                    # Code smell
                    (r'TODO|FIXME|XXX|HACK', 'Unresolved TODO/FIXME comment',
                     'Address or create an issue to track'),
                ]
```

---

#### ./agents/bug-bot/bug_bot.py:149

**Category:** logic  
**Issue:** Unresolved TODO/FIXME comment  
**Confidence:** 80%  
**Fix:** Address or create an issue to track  

**Code:**
```python
                    # Code smell
                    (r'TODO|FIXME|XXX|HACK', 'Unresolved TODO/FIXME comment',
                     'Address or create an issue to track'),
                ]
```

---

#### ./agents/bug-bot/bug_bot.py:149

**Category:** logic  
**Issue:** Unresolved TODO/FIXME comment  
**Confidence:** 80%  
**Fix:** Address or create an issue to track  

**Code:**
```python
                    # Code smell
                    (r'TODO|FIXME|XXX|HACK', 'Unresolved TODO/FIXME comment',
                     'Address or create an issue to track'),
                ]
```

---

#### ./agents/bug-bot/bug_bot.py:149

**Category:** logic  
**Issue:** Unresolved TODO/FIXME comment  
**Confidence:** 80%  
**Fix:** Address or create an issue to track  

**Code:**
```python
                    # Code smell
                    (r'TODO|FIXME|XXX|HACK', 'Unresolved TODO/FIXME comment',
                     'Address or create an issue to track'),
                ]
```

---

#### ./agents/bug-bot/bug_bot.py:149

**Category:** logic  
**Issue:** Unresolved TODO/FIXME comment  
**Confidence:** 80%  
**Fix:** Address or create an issue to track  

**Code:**
```python
                    # Code smell
                    (r'TODO|FIXME|XXX|HACK', 'Unresolved TODO/FIXME comment',
                     'Address or create an issue to track'),
                ]
```

---

#### ./agents/bug-bot/bug_bot.py:149

**Category:** logic  
**Issue:** Unresolved TODO/FIXME comment  
**Confidence:** 80%  
**Fix:** Address or create an issue to track  

**Code:**
```python
                    # Code smell
                    (r'TODO|FIXME|XXX|HACK', 'Unresolved TODO/FIXME comment',
                     'Address or create an issue to track'),
                ]
```

---

#### ./agents/pr-review-bot/review_bot.py:154

**Category:** logic  
**Issue:** Unresolved TODO/FIXME comment  
**Confidence:** 80%  
**Fix:** Address or create an issue to track  

**Code:**
```python
                    (r'except:\s*pass', 'Bare except with pass hides errors'),
                    (r'TODO|FIXME', 'Unresolved TODO/FIXME comments'),
                    (r'print\(.*\)', 'Use logging instead of print statements'),
                ],
```

---

#### ./agents/pr-review-bot/review_bot.py:154

**Category:** logic  
**Issue:** Unresolved TODO/FIXME comment  
**Confidence:** 80%  
**Fix:** Address or create an issue to track  

**Code:**
```python
                    (r'except:\s*pass', 'Bare except with pass hides errors'),
                    (r'TODO|FIXME', 'Unresolved TODO/FIXME comments'),
                    (r'print\(.*\)', 'Use logging instead of print statements'),
                ],
```

---

#### ./agents/pr-review-bot/review_bot.py:154

**Category:** logic  
**Issue:** Unresolved TODO/FIXME comment  
**Confidence:** 80%  
**Fix:** Address or create an issue to track  

**Code:**
```python
                    (r'except:\s*pass', 'Bare except with pass hides errors'),
                    (r'TODO|FIXME', 'Unresolved TODO/FIXME comments'),
                    (r'print\(.*\)', 'Use logging instead of print statements'),
                ],
```

---

#### ./agents/pr-review-bot/review_bot.py:154

**Category:** logic  
**Issue:** Unresolved TODO/FIXME comment  
**Confidence:** 80%  
**Fix:** Address or create an issue to track  

**Code:**
```python
                    (r'except:\s*pass', 'Bare except with pass hides errors'),
                    (r'TODO|FIXME', 'Unresolved TODO/FIXME comments'),
                    (r'print\(.*\)', 'Use logging instead of print statements'),
                ],
```

---


---

*Generated by Bug Bot - Intelligent Bug Detection Agent*
