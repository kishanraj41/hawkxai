---
description: HawkAI /builddocker — build the Dockerfile and update docker run
globs: grokhackx/{Dockerfile,.dockerignore,docs/RUNBOOK.md}
alwaysApply: false
---

When the user invokes `/builddocker` or asks to rebuild the HawkAI image, follow `grokhackx/.cursor/skills/builddocker/SKILL.md`.

build the dockerfile and update docker run. Tag `hawkai:latest`. Restart the `hawkai` container on host 3001 unless told otherwise.
